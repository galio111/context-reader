# 首页重设计实现审计

更新时间：2026-08-20

这份文件只回答一件事：已经确认的产品决定，在真实根首页与 Reader 中究竟实现到了什么程度。`已接入` 不等于视觉通过，复杂动效只有在真实浏览器关键帧对比并经用户确认后才可标为 `已验收`。

## 状态定义

- `已验收`：功能、视觉与重复使用体验都通过。
- `已接入待验收`：真实数据和真实功能已连接，但视觉或动效仍待用户确认。
- `部分接入`：只有一部分状态或表面，不能宣称完成。
- `未实现`：仍停留在决定或问卷层。
- `外部阻塞`：需要资质、账号或第三方服务，代码不能代替外部条件。

## 当前矩阵

| 范围 | 状态 | 当前事实 | 完成前仍需做 |
| --- | --- | --- | --- |
| 裸域名规范首页 | 已上线，待旅程验收 | 大陆生产根路由直接服务 SSR 外刊、真实导入、真实 Menu 和 Reader 回调；旧 `/home-v2` 保留查询参数后以 308 跳到根首页；站内返回、PWA 与离线入口均已切换，没有 `/home-lab` | 用户浏览器地址栏确认、PWA 已安装实例升级、真实登录旅程和全量回归 |
| 游客与登录用户分流 | 已接入待验收 | 游客有封面叙事；登录用户使用较小继续阅读与较大导入的工作台；首页在首次账号解析后锁定本次旅程，中途登录不再强制换工作台；增加 `?preview=guest/member` 无退出预览 | 真实账号中途登录、Reader 中途登录、退出后状态矩阵复验 |
| Ballpit 封面 | 部分接入 | 自由碰撞保留；同一全视口画布从实时位置先让出中央通道、最后离场；滚动几何不再逐帧测量，恢复中途进度时同步控制器 | 0/25/50/75/100% 关键帧、连续往返、用户设备卡顿、手机与低动态实测 |
| 封面到外刊 | 部分接入 | 自然滚动直接驱动可逆时间线；点击复用稍快平滑滚动；外刊同步上移 | 球体两侧分流、最终消失时机和背景融合仍未获用户视觉确认 |
| 中文排版 | 部分接入 | 已把中文标题与英文标题分离，收敛字号、字距和行高；封底只在语义边界换行 | 第 19 轮 248–254 的视觉方向与多屏宽截图验收 |
| 精选外刊布局 | 已接入待验收 | 主推一篇 + 三列标准文章；桌面最多约十篇；移动端两列并减少数量；登录用户可原地展开库存并按分类、难度、搜索筛选 | 用真实外刊数据验证满行规则和长库存性能 |
| 个性化推荐 | 已接入待验收 | 外刊区独立控制同时出现在游客、登录与移动端；五档水平、八类兴趣只重排“推荐”；游客本机持久化，账号进入协议 2 同步 | 真实账号跨设备合并、真实文章分类样本排序与用户视觉验收 |
| Admin 首页外刊编排 | 已接入待验收 | 发布与首页槽位分离；Admin 分类标签、拖拽/键盘排序、推荐首槽主推均接真实服务端接口，生产 schema 已应用 | 用 active admin 实测保存与 SSR 首页回放 |
| Lusion 外刊表面 | 部分接入 | 加入指针惯性、局部图层、滚动速度形变与进入视口显现 | 仍需逐帧对照两段参考录屏；当前不能称为完整复刻 |
| 外刊进入 Reader | 部分接入 | 点击会预取并使用同一张封面展开；文章数据进入真实 Reader | 共享封面在 Reader 顶部的连续落位、反向回卡、快慢节奏矩阵 |
| 自主导入 | 已接入待验收 | 正文/网址切换、各自草稿、URL 预览、错误回退、Ctrl/Cmd+Enter、游客额度接口；Reader 内导入覆盖层保留原会话并复用同一额度 | 导入工作层最终视觉、PDF 后续入口 |
| 桌面高频入口 | 已接入待验收 | 首页已有导入、查词、生词本、我的文章四个真实按钮；游客悬停出现、登录固定；Reader 使用返回按钮下方固定纵列 | 桌面宽屏与重复打开的真实视觉验收 |
| 手机 Menu | 部分接入 | 底部面板已有高频两列与低频列表结构 | 手势关闭、真实高度与手机录屏验收 |
| 单独查词窗口 | 部分接入 | 首页与 Reader 桌面均接真实字典，可移动、缩放、隐藏并跨页面记忆位置；Reader 右栏已移除重复查词标签；手机为高底部面板 | 吸入按钮关键帧、额度状态行视觉、不同宽度默认位置验收 |
| Reader 左侧外壳 | 已接入待验收 | Reader 无品牌胶囊；返回按钮与导入、查词、生词本、我的文章组成左侧纵列；导入与文章库覆盖 Reader 且不卸载正文；宽屏正文为查词窗口预留左侧空间 | 顶部旧文章动作与新外壳的视觉统一、桌面/平板/手机最终验收 |
| Reader 来源恢复 | 部分接入 | 已有来源滚动快照、最多 5 层 Reader 会话、浏览器后退一致、保存/临时阅读进度 | 外刊反向共享转场、筛选/搜索工作层快照、下架卡片回退 |
| 功能介绍卡片 | 已接入待验收 | 七张较大卡组成可点击、拖拽、快速跨卡和无限循环的 3D 环；卡内使用真实能力缩略 UI | 最终尺寸、透视、卡片悬停、手机与低动态验收 |
| 封底与联系 | 部分接入 | 已上线真实邮箱、微信号、原始二维码、原位反馈表单和页脚；二维码缩为微信号右侧精确触发的小浮图；ICP 与公安备案号及官方查询链接均已进入页脚 | 正式文案、跨输入方式和整段构图验收 |
| Menu | 部分接入 | 桌面紧凑浮层、详情向左展开、手机底部面板、点击外部/Esc/关闭 | 桌面最终尺寸、账户首层与详情视觉、登录/退出路径验收 |
| 注册资料与账户 | 部分接入 | 生产 schema 已增加兴趣、出生年份和性别；注册基础账号后进入可跳过第二步；支持五档水平、八类兴趣、出生年份和性别；账户详情可修改水平/兴趣并清除人口资料；退出登录保留 | 真实注册、跨设备同步和清除验收；用户自助修改昵称与六位密码仍未实现 |
| 手机验证码 | 外部阻塞 | 当前仍是未验证手机号 + 六位数字密码 Beta，不发送短信 | 企业短信资质、供应商与第 19 轮账号安全选择；不能伪造完成 |
| 游客额度与 Admin | 已接入待行为验收 | 生产 SQL 已应用；未登录会话接口返回文章查询 10、单独查词 5、正文导入 2、URL 导入 2；Admin 分项配置保存后影响新请求且保留服务端审计；SQL 完成 cache-hit 退款 | 验证北京时间重置、缓存退款、并发幂等和 active Admin 修改 |
| 性能与低动态 | 部分接入 | Ballpit 已脱离 React 每帧重渲染；高刷更新从现场约 150–165Hz 限到约 82Hz，普通屏保持约 60Hz；减少球体几何/像素/数量并缓存指针边界；字母画布改为事件驱动、空闲停帧；IntersectionObserver 控制卡片显现；有 reduced-motion 基础样式 | 用户原设备复验、连续打开/返回 5 次、低性能设备、手机、滚动中段与长列表实测 |

## 当前禁止宣称

- 不能把目前首页称为“最终真实稿”。
- 不能把普通 CSS 倾斜称为“完整复刻 Lusion”。
- 不能因为页面能打开、构建成功或 HTTP 200 就说视觉一致。
- 不能在 Reader 全来源恢复、登录开场、反向共享转场、账号自助改密和复杂视觉未完成时说两百多项问卷已经全部落地。

## 2026-08-20 生产评审基线

- 裸域名规范首页 release `20260820T234040`，父版本 `20260820T230128`，源码 `f331e9c308df54e63ee4e60a95d1588b68993cb3`；根首页公网返回 200，旧 `/home-v2` 返回保留查询参数的 308。
- 大陆生产 release `20260820T230128`，父版本 `20260817T221932`，源码 `3f2c56ba8d68e9fc14436bf571e1cb103ad6965d`。
- 生产构建、release contracts、77 项精确文件差异、七服务健康、PostgreSQL 新备份与隔离恢复均通过；旧接受镜像 `accepted-20260817T221932` 与源快照保留。
- 公开推荐接口返回 13 篇文章；游客会话返回四个新额度池；未登录 Admin 数据接口继续返回 401。
- 这次上线只把当前真实代码变为后续共同评审基线，不代表复杂动效、中文文案或全部问卷视觉已经验收。

## 2026-08-18 需求对齐纠偏结论

本轮由用户指出“外刊个性化推荐/英语水平”完全漏接后，重新从当前决策总表逐项反查真实代码。确认这不是单点遗漏：个性化推荐、登录用户完整库存、Admin 首页槽位、注册第二步、账户阅读资料、游客额度文档和首页中途登录一致性此前都存在“记录过但未闭环”的情况。本轮已把这些功能接入或明确降级为待生产/待真实账号验收，不能再在报告中统称“需求已经完善”。

仍然明确未完成的已确认需求包括：Lusion 共享图像进入与反向回卡的逐帧复刻；所有 Reader 来源工作层的完整快照恢复；用户自助修改昵称与六位密码；登录 Ballpit 最终视觉和低性能设备验收；手机 Menu 下滑手势与若干复杂动效；正式文案。手机号验证码、找回密码和短信资质已被第 19 轮暂缓，不属于本轮漏做。
