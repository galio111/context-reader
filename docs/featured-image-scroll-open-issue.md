# Featured image scrolling and article return: mitigation shipped, visual acceptance open

Current correction: `20260923T161000` (parent `20260923T150500`, source `aeba5ab75060feb7b0bb122072e60ee4eeb67547`) restores the original stored high-resolution cover for every nearby or pointer-entered card. Inline previews are actual image elements sharing the original tilt/hover/reduced-motion styles, rather than a flat background; the original overlays them after load. A mounted component test exercises pointer rotation/reset and the high-resolution handoff. Public SSR contains preview img elements and the exact mainland release identity is verified. The user explicitly protects image visuals, motion and high-definition clarity against any performance tradeoff; this is recorded in AGENTS.md and durable memory. Browser access still failed, so rendered visual acceptance is open. The previous missing server-only test bootstrap was fixed by the intervening CET release and core regressions now pass.

The user observed gray cover slots and later generic source artwork while rapidly scrolling the authenticated featured-article library. Existing image URLs and successful HTTP responses did not establish timely painting. The user also observed returning from an article at the bottom feedback section instead of the card they opened.

Release `20260923T090800` (parent `20260923T031100`, source `cb1164249a878926f52c7b1546c2ad1c4c709b80`) changed recommendation metadata to embed a 256×192 WebP preview of each stored cover. That intermediate release used the preview as the ordinary card image, which the user rejected because it permanently reduced sharpness and left the visible preview outside the img transform layer. The current correction restores original full-cover preloading for all nearby cards and renders both preview and full cover as transformed image elements. Expanded member lists wait for the complete catalogue before rendering. That release also disabled expanded-card reveal and pointer motion, a regression against the accepted homepage contract. The public-article return path records the clicked card and its viewport offset, then restores that relative position after the homepage view and catalogue have settled. If the card is no longer present, it returns to the article grid rather than the feedback section.

Before backfill, a fresh PostgreSQL backup at `/var/backups/context-reader/postgres/daily/context-reader-20260923T090459Z.dump` was isolated-restored across 16 public tables. The 479 existing first-party covers were backfilled transactionally with unchanged editorial timestamps. The live public catalogue has 481 articles, 479 stored covers and 479 matching previews. Two older NPR rows remain text-only. The full JSON response is 4,617,300 bytes and about 3,327,305 bytes over gzip.

Release `20260923T125000` (parent `20260923T090800`, source `edfed9e3f301980653affd486b7d719d06c4895e`) added a 60-second local service-worker catalogue cache and conditional ETag revalidation, plus a distinct request key to avoid the previous 96px cache entry. Release `20260923T130100` (parent `20260923T125000`, source `0500e8908dd664301498ef129aa1274a5949eef1`) accepts Next.js's compressed `-gzip` ETag during revalidation. The live response now returns 304 with zero body bytes; two simultaneous conditional requests completed in 0.45–0.47 seconds. A new or expired catalogue still needs transfer: two simultaneous cold gzip requests took 17.68 and 20.44 seconds. The local cache helps repeat visits but does not remove this bandwidth limit.

Release `20260923T133000` (parent `20260923T130100`, source `00e106ecf4bd57a6f2c525f5080780d5e160e195`) restores the same clear 0.90-to-1 cover entry for featured, ordinary and expanded cards. A shared IntersectionObserver clears the visible state after exit so entry replays in either scroll direction; pointer response again follows the existing user preference across the expanded grid. The delay remains capped at the last showcase card. The project `AGENTS.md`, homepage decision register and implementation contract now prohibit suppressing existing visual/interaction behavior as a performance shortcut. A focused DOM regression covers 420 expanded cards, exit/re-entry and observer cleanup; the stable release guard checks that expanded motion cannot be silently disabled. The public CSS includes the 0.90 scale state and no expanded-library suppression rule.

The production build, focused motion test, release contracts, static-egress audit, catalogue service-worker test, seven-service health, mainland release identity, public homepage/article detail and anonymous Admin/account boundaries passed. The accepted current and parent app images and latest verified backup remain available. The full critical suite is blocked before tests run by the existing missing `server-only` module in standalone tsx. Browser automation returned no browser surfaces (`nodeRepl.fetch request failed`), so authenticated desktop/mobile rapid-scroll frame stability, the appearance of the restored motion and the return-to-card viewport were not visually verified. The user's requirement of immediate photos at any scroll speed for two cold concurrent visitors is not yet met. Further work needs either a substantially smaller first catalogue, progressive loading with an explicit limit on how far the user can scroll before data arrives, or more delivery bandwidth/CDN; none should be called a zero-wait guarantee before a real cold two-user browser test.
