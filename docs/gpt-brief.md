# Context Reader GPT 项目上下文包

核验日期：2026-09-20。GitHub `galio111/context-reader` 的默认 `main` 是 ChatGPT 读取当前代码与文档的入口；任务分支仅推送成功不代表默认入口已更新。先读 `AGENTS.md`，再按问题读本文件链接的专题文档。

## 当前版本与范围

大陆生产 release `20260920T104100`，parent `20260920T101600`，source `86f4a55840e176a60b74189e1cd771fde685c1fe`。累计保留词典关联词/历史前缀检索及文章媒体修复；新增手机划词 AI 追问和 25–82dvh 可停留的下边栏，低于四分之一屏或快速下甩关闭。

89 项关键回归、4 项媒体回归、1 项手势回归、构建及发布契约通过。独立 Chromium 已在公网验证 390×844 触摸拖动（四分之一实测 211px）、快甩、重新打开、追问上下文与桌面/手机日夜显示；UI 测试使用固定解释/回答，另用临时真实登录账号验证查词、词典、全文翻译和追问成功并删除测试账号。账户/同步/Admin 边界、七服务健康、16 表备份恢复和父版本回滚镜像通过。真机手感仍待用户验收；此前媒体任务的视觉边界见历史记录。发布规则见 [release-governance.md](release-governance.md)，历史见 [product-journey.md](product-journey.md)。

## 产品与架构

Context Reader 帮助中文母语用户阅读真实英文文章。阅读为主；语境解释、独立词典、翻译、生词与 Anki 支持阅读。技术为 Next.js 15、React 19、TypeScript，AI 模型配置以源码及环境覆盖为准。

- 正式地址 `https://context-reader.com`；`/` 唯一首页，`/home-v2` 保留参数永久重定向。
- `app/page.tsx` 服务端读取外刊摘要；`HomeClient` 负责文章、缓存、入口及 Reader 会话；`HomeRedesign` 是当前首页，旧 `BookHome`/`CurvedPageTurn` 仅为遗留代码。
- 腾讯云上海同机运行 Caddy、应用、PostgreSQL 17、GoTrue、PostgREST、Storage 和内部兼容网关。生产 `SUPABASE_URL` 必须是 `http://supabase-api:8000`。
- Vercel 旧完整应用和托管 Supabase 是冻结回滚参考。独立海外 fetch 服务仅处理受约束的公开网页/feed/robots，不承载账号或产品数据。
- 文章、生词、阅读状态、词典和缓存逐条写入 IndexedDB；localStorage 仅保留小型设置/身份/修复标记。旧数据先备份、校验迁移，不要求用户清站点数据。
- 登录完成与后台恢复解耦。协议 2 使用固定快照、opaque cursor、CAS、manifest 和 tombstone；只上传变化对象，恢复可续传。普通同步静默，真实本地写失败才显示备份/重试。

详情见 [architecture.md](architecture.md)、[integration-guide.md](integration-guide.md) 和 [account-usage-plan.md](account-usage-plan.md)。

## 当前产品行为

- 游客路径：Ballpit 品牌开幕 → 五模块录屏功能展示 → 精选外刊 → 正文/网址导入 → 联系反馈。登录用户新开页面进入粒子文字开场后的阅读工作台，途中登录不抢走正在阅读的位置。
- 手机保留少量背景球，明确上滑可对齐功能展示；探索仅直接打开说明，物理吊牌限桌面。普通工具层可拖动停留在 25–82dvh，低于四分之一屏或快速下甩关闭；手机划词解释也开放 AI 追问，局部滚动不串到正文。
- 首页独立查词按 2026-09-15 明确纠偏支持桌面拖动和调大小，不跨会话保存坐标；手机仍为底部面板。该决定覆盖此前固定窗口限制。
- 文章点击词或有意横划/长按短语查询；纵向移动始终是阅读滚动。IPA 和发音属于实际当前词形；流式、完成态、缓存回放保持内容一致。
- 全文翻译只由侧栏内明确动作启动；普通正文使用有界批次、逐块显示和保存。切换工具保留任务和译文滚动，重翻逐段替换而不先清旧结果。
- 保存先保住文章；首次缺有效摘要时才生成，超过 50,000 字符或失败仍可保存。保存后仅禁用的“已保存”，没有摘要重写。
- Reader 原位编辑，保留空段；撤销/重做属于阅读会话。“返回首页”回根来源快照，浏览器 Back 才逐篇恢复。长文只在视口附近挂载交互 token。
- Anki 以稳定 ContextReaderId/历史身份恢复导入回执，防止断线重试重复建卡；新卡需 US/UK 音频齐备。已有卡不被自动批量改写。
- TTS 复用私有 Storage、32 MiB/1000 条进程热缓存和 in-flight 请求；存储瞬断有限重试，写失败后台补存；重启/淘汰期间仍可能重复合成。

## 账号、额度与安全

手机号只是未验证的大陆手机号标识，不发送短信，不证明归属。注册只收昵称、手机号和密码；新密码/主动改密须 8–72 个不含空格的可打印 ASCII 字符、至少一字母和一数字。旧六位数字密码仅兼容登录。可改昵称/密码；自助找回待真实归属验证，不能通过昵称代替。

刷新 Cookie 滚动最长 400 天，但仍受上游会话、禁用、改密和撤销约束。普通退出先成功同步，使用 local scope，不退出其他独立设备会话。会话校验失败进入明确离线受限态，不能变成游客或恢复服务端权限。

- 游客上海日独立池：文章查询 10、独立词典 5、粘贴导入 2、URL 导入 2；缓存、失败、及时取消不扣。
- 注册用户的摘要和全文翻译是独立月池。新翻译任务算一次，缓存重看免费，明确重翻另算；精选预载译文首次点击算动作但模型成本为零。
- 所有登录账号均可见实际余额与北京时间重置点，独立于商业营销开关；会员耗尽不能提示“游客额度”。不完整流不能先结算成功，额度拒绝不再触发重复 fallback。
- 邀请码单次兑换 Basic/Plus/Max 有期权益；线上付款尚未接入。
- Admin 正常入口和 `/api/admin/*` 只认服务器 active admin entitlement，旧密码会话仅恢复用途。密钥不下发浏览器。
- 远程抓取保留固定 DNS、私网拦截、大小/类型限制；详细故障私存，网络/输入错误不算开发错误。

## 推荐与未完成事项

推荐栏是 Admin 白名单，偏好只在允许集合内重排；取消的文章不能由全库补回。手动候选统一进入真实 Reader 编辑与审核；独立自动精选模式通过全文、图片和难度审核后直接发布。自动候选至少 401 英文词及安全保存配图；站点和日目标属 Admin 运行配置，不能把历史来源表当实时名单。自动精选目标每天 35 篇，低难度不计入；不足不凑数。最新上线、开关和全库复审结果以 automated-editorial.md 为准。

OCR 仅存兼容代码，所有用户入口关闭。短信归属与密码找回、支付、异地备份完善、其余展示录屏素材、实机多端迁移/音频及复杂动效用户验收仍有未完成项，不能据构建通过统称已完成。

首页决定按 [current-decisions](home-redesign-current-decisions.md) → [implementation-audit](home-redesign-implementation-audit.md) → [interview-archive](home-redesign-interview-archive.md) → 历史读取。Lusion 逐帧视觉还原与重复使用体验仍须单独验收。

## 开发、发布与 GitHub 同步

使用 `scripts/new-task-worktree.ps1` 创建独立 `codex/*` worktree。生产从当前 accepted source 累计集成，构建、关键测试、契约检查后通过固定服务器发布入口；只重建 app/caddy，公网精确版本及相关回归通过才算部署完成。

完成验证后自动提交任务分支，并将累计接受版本与文档合入默认 `main`，普通非强制 push 后用 `git ls-remote` 验证远端 main 等于审查提交。保留并发分支，不上传日志、缓存、发布包、密钥和共享目录未完成改动。不以推送代替生产验收，也不以生产上线代替 GitHub 同步。


## 智谱备用模型（2026-09-19 已上线）

DeepSeek 保持首选；查词/划词及全文翻译继续默认 Flash，其他模型配置不变，不做 Flash/Pro 互切。共享服务端 transport 在繁忙、连接失败、空响应或 6 秒没有首段有效输出时转智谱 `glm-4.5-air`（关闭思考）；非流式请求以完成响应为超时边界。故障后进程内避开 DeepSeek 30 秒，取消不触发备用，复用原扣费动作。已显示内容不拼接另一模型，保留结构化补救/缺失段落续译机制；独立词典中途断流仍可能需要重试，不能承诺所有故障完全无感。release `20260919T230100`，parent `20260919T223456`，source `f012229a94e660799eb3de0df4286e2e5ca8d6da`；公网版本、强制故障下两类真实流式请求与各扣一次额度、账号同步、Admin、健康、备份恢复和回滚镜像均已验证。

后台账号面板在“每日运行情况”下显示“智谱备用调用”，按最近 30 个上海自然日的执行账本统计调用、成功/失败、token 和成本，列出最近 100 条智谱记录。点击“刷新数据”更新；此统计不声称覆盖尚未写账的失败连接尝试。

上述后台展示已由 release `20260919T232800` 上线（parent `20260919T230100`，source `5fa612eb68836158d5605f9c5fc4c703a14dc4e5`）；管理员接口实测显示两条成功的智谱验收调用，普通登录阅读核心三项接口保持 DeepSeek Flash 成功。

自动精选与 DeepSeek/Jev 接口准备、质量门槛、成本及验收边界见 [automated-editorial.md](automated-editorial.md)。20260920T104100 已接受：先修复可安全移除的非正文内容，再全文及图片复审；旧版提取误删正文、推荐广告误判已修复。9 月 20 日实测自动精选 30 篇，首页 HTML 可见 30 篇，64 张存储图片全部可读，成功统计邮件已发。北京时间每日 06:00 开始，正常上限 150 次尝试，手动候选流程保留；达到每日目标后停止付费储备，并保留失败告警。当天难度六级/考研 15、进阶 15、基础 0，不改标签凑分布；长期稳定供给仍需运行证据。Jev 已于 9 月 20 日配置到生产并选择对照模式，服务器模型列表验证通过；当天预算已耗尽，尚未进行实际推理，DeepSeek 仍终审。

自动精选成本和商业分类更新的部署状态、¥1/日费用边界、35 篇条件估算以及 Jev 接入步骤见 [editorial-cost-and-jev.md](editorial-cost-and-jev.md)；历史全库离线分类的费用记录不完整，不能把局部 token 估算当作整个账号账单。

最新生产：20260920T172600，父 20260920T104100，源码 0a721d9997f9052b7d7d94dd433246098ab7cb93。自动精选目标 35、¥1/日，达标后停止付费储备；分类须全文证据，商业偏好改用元数据，六级/考研与雅思基础合并偏好层。Guardian 与 Crunchbase 已验证启用。今天实际 30/35，因先前费用约 ¥1.638 已超新限额，未加费补数；费用不足邮件已发，长期 35/日仍待真实运行验证。
