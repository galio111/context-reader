# Context Reader GPT 项目上下文包

首页重设计继续讨论前，优先读取 `docs/home-redesign-current-decisions.md`；需要追溯用户每轮原始选择时读取 `docs/home-redesign-interview-archive.md`。历史问卷和产品历程可以保留被覆盖答案，不能反向覆盖当前决策总表。

这是一份给无法读取仓库的对话使用的精简上下文。能连接 GitHub 时，优先读取 `galio111/context-reader` 当前分支的 `AGENTS.md` 和相关代码；GitHub 看不到本地未提交改动、浏览器数据、Codex 记忆和私有生产状态。

如果本文与代码、`AGENTS.md`、`PRODUCT.md` 或 `docs/architecture.md` 冲突，先让 Codex 核对当前工作区。回答时必须区分“已经实现”“已接受但待实现”“候选想法”和“已放弃方向”。

## 1. 项目与生产

Context Reader 是面向中文母语英语学习者的真实文章阅读工具。用户粘贴英文文章、导入公开 URL 或打开推荐/保存文章，在原文中点击单词、划选短语、查看中文语境解释，保存生词，并可使用全文翻译和 Anki。

- 正式地址：`https://context-reader.com`
- 正式主页：裸域名根路由；旧 `/home-v2` 只作为兼容入口跳回根首页
- 技术：Next.js 15、React 19、TypeScript、DeepSeek、本地优先浏览器数据、AnkiConnect
- 生产后端：腾讯云上海上的 PostgreSQL 17、GoTrue Auth、PostgREST、Storage 与内部兼容网关
- `context-reader-ten.vercel.app` 和原托管 Supabase 仅作回滚/参考

## 2. 产品目标与性格

阅读是主体验，查词、翻译、生词本、推荐、账号和 Anki 都是支持工具。成功不是“功能很多”，而是用户能在真实英文里更少中断地理解、积累和继续阅读。

阅读器应安静、清晰、适合长期使用；首页可以更有表现力，但必须快速进入真实阅读，不能变成通用 AI 官网、游戏、社交信息流或只在第一次惊艳的动效展示。

用户对质量要求很高：自己不满意就不会给别人使用。设计讨论不应急着实现；先同步视觉与功能意图，降低重构后的“开盲盒”和反复消耗。

## 3. 当前核心流程

1. 从粘贴、URL、推荐或已保存文章进入。
2. `ReaderView` 展示纯文本或结构化文章；URL 可保留标题、段落、列表、引用、有效图片和 `sup/sub`。
3. 点击单词或横向选择短语，流式解释直接长成最终面板。
4. 保存生词，保留音标、原句、上下文含义和 Anki 数据。
5. 在侧栏按需启动全文翻译；按文本 block 逐段完成并立即缓存。
6. 在阅读画布原位编辑文章，必要时删除整张图片 block。
7. 本地数据在登录后通过协议 2 同步到云端。

## 4. Reader 不可破坏边界

- 文章文字始终是视觉锚点。
- 编辑不能换成 textarea 或独立卡片编辑器；阅读/编辑必须共享字体、宽度、换行、行高和段落间距。
- 不要每次按键都用 React 受控 state 重画 `contentEditable`，否则光标会跳。
- 显式空段落是内容，必须保留；图片只读但可整块删除。
- undo/redo 是一次阅读会话的全局历史，保存后仍可用，回主页后结束。
- 手机竖向手势优先滚动；短语选择需要明显横向意图或长按。
- 返回主页、切换文章和源句定位要正确重置或恢复滚动，不让旧状态吞掉新请求。

## 5. 查词、词典与全文翻译

- 文章查词只发送选中词/短语、当前句、前一句和后一句。
- 缓存未命中时结构化与流式解释并行；流式可见字段是权威，完成后合并耐久字段并缓存，不能突然换成另一套内容或布局。
- IPA 必须明确属于用户实际选择或输入的英文词形，并以 `phoneticFor` 校验；`原型` 与 `当前词音标` 分开显示，音频始终播放当前词。旧数据缺少归属且当前词与原型不同时隐藏 IPA，不能猜测补写。
- 独立词典支持中文词/短语、单个英文词或不超过 8 个词的英文短语；中英混输、过长输入和整句应在请求前说明不支持。
- 拼写错误只显示纠正，不得把正确词义挂在错误词头下，也不得进入历史或生词本。
- 圆形重新生成绕过缓存；若同词同原句已存生词，则更新生成字段但保留 id、创建时间和 Anki 记录。
- 全文翻译只能在侧栏内由用户启动。切换工具或短暂离开不能取消任务。
- 一次请求一个文本 block，但携带整篇 `contextBlocks`。每完成一段就保存；刷新、换设备或重开推荐后只续缺失段。
- 修改文章后只更新变化 block。强制重翻也不能先删旧译文；新段成功后逐一替换。

## 6. 首页：当前状态与已接受方向

### 当前已实现

`app/page.tsx` 服务端加载公开推荐摘要并交给 `HomeClient`；`HomeClient` 负责真实文章状态、预取、缓存和 `ReaderView`。大陆生产已经由根首页的 `HomeRedesign` 接管，并连接 SSR 外刊、导入、Menu、Reader 与来源会话栈；这只是评审基线，复杂动效仍未完成验收。

### 已接受方向与当前本地实施状态

以下列表同时包含已接入待验收、部分接入和仍待实现目标；必须以 `docs/home-redesign-implementation-audit.md` 的逐项状态为准，不能把整组误称为已完成。

- 保留现有封面和 Ballpit 球体碰撞效果，作为封面/品牌 IP。
- 舍弃全部封面翻页、内页翻页、滚轮翻页和自动收尾。
- 新用户/未登录用户顺序为“品牌封面 → 精选外刊（约 10 篇、约两屏；1 篇全宽主推 + 3 个完整三列标准行，可切换类型）→ 粘贴正文/输入网址 → 功能简介 → 联系/反馈”。数量允许围绕完整行小幅调整，不能留下单篇残行。精选外刊与自主导入是并重入口，语境解释/翻译仍是核心能力。
- 新打开站点时，登录用户进入快速路径。有记录时较小的继续阅读与更突出的导入器在同一工作台；最近文章列表不再重复，只保留“我的文章”入口。无记录时隐藏空继续阅读，导入器成为主景，外刊紧随。登录用户不重复功能介绍卡片。手机保持顺序，不要求都塞进同屏。
- 登录只更新身份与权限，不能抢走当前旅程。游客在首页或 Reader 中登录后保持原页面、文章、分类和滚动位置；下次重新打开网站才默认进入登录路径。退出登录后显示游客首页。当前账号页已有退出按钮，重设计必须保留。
- 登录用户每次全新打开或刷新网站时播放彩色粒子拼字加载动画；同一浏览中的内部返回和游客中途登录不播放。粒子从上方掉落，在中央清晰组成 `Context Reader`，只大写 C/R，保留正常单词间距。`FallingWordOpening` 已重采样清晰词形并在本地生产构建通过中段可读检查；手机、低动态和用户设备仍待验收。旧 Ballpit 螺旋卷入、球团与开孔登录动画已经舍弃。
- 新用户封面采用约 82–88vh 的完整品牌门面，底部露出精选外刊一线；静态结构是有留白的门面、无明显边框的中心球场、左侧克制文案，不是满幅球幕或常规左右分栏 Hero。第一屏主文案为“在语境里，读懂英文。”，并配一句克制说明和一个进入动作，但副文案、动作标签、行宽与断行均未定稿。封面背景仍待在纯净近白、极淡品牌色环境光、极淡图像三种可见方案中选择。
- 封面 Ballpit 略微透明；群体质心以很弱的力回到右侧宽活动区，单球保持随机热扰动、碰撞、鼠标响应和自由越界。向下滚轮与“精选外刊”按钮共用约 `1.9s` 的吸附时间线，向上倒放吸回封面；后续页面仍自然滚动。精选外刊同时从下方逐渐上移，列表完全就位的终点球体才消失。
- 推荐展示按用户提供的两段 Lusion 录屏高保真复现。桌面游客橱窗约 10 篇、约两屏，第一篇主推独占首行，之后严格每行 3 篇，不留单篇残行；默认分类是“推荐”，后面才是时事、科技、文化、商业等，各分类首页数量相近。分类切换是相同槽位的旧内容退场与新内容接续，不是瀑布流或追加。图片明显小于参考并使用 `4:3`，但局部 3D 与共享转场保持完整录屏强度。静止信息显示标题、来源、难度与阅读时长；色场自动提取封面低饱和主色；进入 Reader 后封面成为文章顶部持久内容，不自动淡出或卸载，只随正文自然滚出视口。手机主推全宽、其余两列且数量可减少，一次点按直接进入。
- Reader 不显示 `Context Reader` 品牌胶囊，左上按钮保持当前位置并始终写“返回首页”，但实际按来源快照恢复：外刊恢复分类/筛选/库存/滚动/卡片，继续阅读恢复工作台模块，我的文章与生词本恢复工作层/搜索/滚动/选中项；首页不重复最近文章列表。粘贴返回输入区但清空，URL 返回保留网址和预览。继续阅读打开时恢复上次正文锚点。Reader 内最多保留当前标签页 5 篇会话栈，浏览器后退与页面按钮同序。刷新直接回首页，已保存进度可继续。登录用户保留最近一篇未保存导入文章到本地临时槽位，下一篇替换，继续阅读标记“未保存”；游客不保留。主动退出登录结束 Reader 并去游客首页。
- 外刊卡片、转场层与 Reader 顶部封面表现为同一张图的连续共享对象，不同时绘制重复图片。点击立即并行请求正文，数据按时到达则只用 Lusion 转场完成进入，不显示通用加载动画；超时只在已落位封面下显示正文骨架。登录站点 Ballpit 开场与外刊进入互斥。游客反向约 `1.8–2.0s`，登录用户约 `0.9–1.2s`；非卡片来源使用 `180–260ms` 克制转场。
- 游客只看到 Admin 专门编排的首页外刊橱窗。Admin 采用分类标签页、可拖拽槽位和固定首槽主推，分别控制默认“推荐”及各分类的文章和排序。登录用户在同一橱窗点击“显示更多”后，剩余库存直接在当前首页下方延展，提供分类、难度和搜索，不跳转、不覆盖，也不显示“完整外刊库”标题；这一长列表是登录用户首页最后一段。
- 点击推荐直接进入真实 `ReaderView`。
- 游客桌面首页的品牌胶囊悬停/聚焦后浮出输入文章、单独查词、生词本、我的文章四个按钮；登录桌面首页和 Reader 则固定显示四个按钮。首页直接打开的生词本与我的文章从按钮列右侧安全间距后开始，打开期间不显示按钮说明气泡；Reader 的独立面板保持自己的既有左侧定位，不复用首页偏移。单独查词复用完整 `BookDictionary` 单实例，默认小窗已改成上方输入、下方独立结果，位置与尺寸浏览器长期记忆；手机从 Menu 打开高底部面板。鼠标彩色字母默认只在非 Reader 首页运行，可在 Menu 设置关闭。
- Reader 内导入新文章时保留旧阅读快照；新文章在当前 Reader 打开，明确返回动作恢复旧文章、滚动位置和解释面板状态，未提交编辑不得静默丢失。
- 日间和夜间是同等完整的产品状态。任何新增或重做 UI 都必须在同一轮定义两套配色，并覆盖默认、hover、focus、active/selected、disabled、loading、warning、error 和 placeholder；独立页面与 Menu 内嵌形态都要检查。普通文字和 placeholder 至少 4.5:1，大字与非文字控件至少 3:1，不能用 Tailwind 类名子串推断交互状态。
- Reader 长文始终使用真实块高度，不允许用 `content-visibility` 固定估算改变滚动条长度。滚轮或滚动条拖动期间只保留纯文本并暂停阅读锚点测量，停稳后才把最终视口附近段落升级为逐词交互；缺少固有尺寸的旧图片先用 4:3 占位，避免懒加载从零高度撑开页面。
- 桌面 Menu 位于右上，打开为紧凑浮层，不重复四个高频入口；未登录状态整行可直接打开登录。详情在同一浮层关系中替换或向左展开。Menu 已增加设置：关闭/开启鼠标字母轨迹，以及日间/夜间首页 UI，偏好在浏览器持久化并作用于游客与登录路径。手机底部 Menu 同样提供设置。
- 账户详情先显示身份、登录与同步状态，再分组显示额度、数据导出和退出。阅读资料已接入：水平/兴趣可改，出生年份/性别可清除；手机号只读。用户自助修改昵称与六位密码仍未完成。注册基础账号后进入可跳过第二步，选填出生年份、性别、阅读偏好、英语水平；只有水平和兴趣参与推荐。
- 年龄只填出生年份；性别只选男/女；二者不参与推荐或公开展示。兴趣八类为科技科学、自然环境、文化历史、社会时事、商业经济、人物成长、故事文学、健康生活；水平为高中、四级、六级、考研、雅思/托福。生产生效仍需应用资料字段迁移并验证导出与清除。
- 外刊区已经接入个性化推荐控制，游客和登录用户均可修改，只影响默认推荐页，不取代内容分类。游客当前浏览器持久化，登录用户进入协议 2 跨设备同步；中途登录的当前页保持游客选择，账号云端选择作为下次默认。控件只承诺定期更新。
- 大陆生产已经拆成北京时间自然日文章查询 10 次、单独查词 5 次、粘贴正文 2 次、URL 导入 2 次四个池；缓存命中/失败/及时取消不扣，四项均由 Admin 可修改。`finalize_usage` 与资料字段迁移已于 2026-08-20 应用；缓存退款、并发与真实账号跨设备行为仍需验收。游客可看公开预发布翻译/摘要，私有全文翻译、摘要、保存、生词本和 Anki 需登录。
- 受限动作或额度耗尽时才打开登录；Menu 也可主动登录。保存或加词触发登录后保持 Reader 和滚动位置，并自动、仅一次完成刚才动作。剩余次数仅在 3、1、0 时于解释面板或词典窗口自身结果区提示。游客偏好与账号冲突时，当前页面保持游客选择，账号偏好作为以后新打开网站的默认。
- 外刊之后用一句克制桥接语和明显留白进入自主导入。新老用户复用基础导入器，默认“粘贴文章”，以“粘贴文章 / 输入网址”切换并分别保留草稿；正文紧凑起步、聚焦或粘贴后展开，保留段落与空行。网址抓取成功显示标题、来源、封面、摘录、字数的只读预览，再确认进入 Reader；失败保留网址并引导改用粘贴。小问号只放三四行抓取边界。未来优先文本型 PDF，扫描版与图片/OCR 均不显示入口；完整隐私说明放使用说明。
- 多行正文 Enter 正常换行，`Ctrl/Cmd + Enter` 提交；提交按钮始终保留。游客首页以留白充分的杂志封底式联系区结束：微信号 `LA060321o` 可复制、用户提供的二维码原图按需展开、反馈原位置展开，之后短页脚含 ICP 备案 `蜀ICP备2026045148号-1`、公安备案 `川公网安备 51010702044616号`、版权、邮箱 `13874807542@163.com`、使用说明、意见反馈。两项备案分别链接官方查询入口。只说持续完善并链接更新记录，不承诺固定频率；更新记录承载仍待确认。
- 桌面 Menu 接近不透明，详情从列表左侧扩展且保留右侧列表，外部/Esc/关闭按钮可关；精确尺寸必须看真实稿。游客首页品牌胶囊下方纵向浮出四个小胶囊，登录首页固定；Reader 返回与四按钮在最左侧同列，查词窗在右侧。手机统一为约 `58–65%` 高底部 Menu，可下滑、遮罩或按钮关闭。
- Admin 分别配置文章查询、单独查词、正文导入、网址导入四项游客额度；保存后对新请求立即生效，不重置已用量，首版只写服务端日志。
- 导入之后使用七张较大的功能卡组成可旋转 3D 环，前方约三张可读，后排形成空间背景；鼠标拖拽、触摸滑动、左右按钮和点击卡片都能无限循环到目标卡，快速拖动最多跨一至两张并保留连续轨迹，不显示线性序号。每卡使用真实小画面、短名称和一句短说明。方向已接受，最终尺寸、透视与手机降级仍待真实稿。
- 使用说明现为不透明浅色的桌面近全屏/手机全屏内容层，持续保留目录；先以真诚简短的“开发者的话”说明长文生词与逐词查找痛点，再顺读三分钟开始、核心特色、阅读操作、Anki、常见问题与更新。它突出精选外刊和自主导入、按水平与兴趣筛选、语境解释、AI 继续提问、全文翻译/摘要和生词复习闭环；没有独立账号/同步/离线章节、页首开始按钮或大块深蓝容器。五个操作项等待用户录屏后转为动图。功能展示区可直接打开说明，生词本 Anki 问号可直达详细章节。
- 不新建 `/home-lab`，也不做脱离数据的静态原型；在实际根首页上从第一视觉切片就接入真实 SSR 数据、HomeClient 回调、账号/Menu 和 Reader 功能。
- 整个网站的 UI 与功能设计仍将继续共同讨论；除以上几点外，不要自行冻结新布局。

当前翻页代码仍存在只是实现现状，不再是必须保护的未来方向。完整契约见 `docs/home-v2-implementation-contract.md`。

## 7. UI 与动效协作方式

用户可以只提供网址、5–15 秒录屏、截图或公开源码，不需要准确说出专业动效名称。实现前，Codex 应先复述：起点/终点、触发方式、位移/缩放/遮罩/层级/形变、节奏、技术不确定点和最小评审切片。

Lusion/共享图片首次评审必须把参考录屏和实现录屏并排，并列 `0/25/50/75/100%` 关键帧；达到参考后连续开合 5 次再讨论快速节奏。后半程已确认使用自然滚动、同一纸面缓慢换色和不同空间接力；手机缩短距离，低动态仅保留色场/布局接续。不能用全站统一淡入冒充完成。

注册现在只保留手机号标识、昵称与密码，不再出现资料第二步；账户已低调支持昵称与密码自助修改。当前手机号不发短信、也未验证归属，因此暂不开放密码找回；等合规短信验证上线后再实现自助重置。更新记录进入使用说明内容区，首页链接直达。

验收分三层，不能混为一谈：

1. 技术正确：构建、HTTP、回调、状态、控制台和响应式正常。
2. 视觉还原：在真实浏览器对比参考与实现的起点、25%、50%、75%、终点关键帧。
3. 产品体验：重复多次完成“主页→推荐→阅读→返回”，检查等待、滚轮冲突和认知负担。

浏览器/参考无法打开时必须标注“视觉未验证”；静态 bundle 推断不是亲眼观察。实现者自检不能替代用户的审美接受。复杂动效只有得到用户明确确认后才能被写成受保护行为。

问卷里的文字选项只代表结构假设，不代表用户喜欢或接受它暗示的 UI。视觉决定要提供可见草图或真实浏览器切片，并把“结构接受”和“视觉接受”分开记录。

## 8. 保存、生词与 Anki

- `SavedArticle.importedArticle` 可选但必须兼容旧数据；存在时保留 rich blocks、图片、inline 上下标和已有 OCR/layout metadata。
- 相同正文和 `-local-recovered-*` 血缘只显示一个逻辑文章，保留最丰富内容和最新打开时间，多余 id 写 tombstone。
- 生词保留 phonetic、source sentence、contextMeaning、例句、用法、搭配和 Anki 记录；按创建时间倒序。
- 大生词本使用真实内容高度虚拟化和预建前缀索引，不要恢复固定大行高或逐条手动展开来掩盖截断。
- 网站发音使用服务端火山 TTS 缓存的统一美音/英音 MP3，不静默换成不同浏览器的 SpeechSynthesis。
- 新 Anki 卡先取得两种云端音频再创建；失败不生成残缺卡。导入时尝试关闭 deck autoplay，写配置失败不阻止 note 导入。
- 生词本顶部只保留批量导入、Anki 设置与问号帮助；连接检测只在设置内出现，批量导入与设置同为浅色描边按钮。问号桌面悬停/聚焦时显示、离开立即关闭，手机点按切换。生词本/我的文章独立面板与 Menu 互斥，外部关闭不闪现 Menu。
- Cloze 正面大留白之后只显示最新耐久 `contextMeaning`，不放 basic meaning、整句翻译、字段标签或模型临时 cue。

## 9. 账号、额度、同步与离线

- 当前公开测试注册：大陆手机号标识 + 昵称 + 6 位数字密码；不发短信，也不证明手机号所有权。内部 synthetic email 不能显示。
- Admin 可私发每人不同的一次性邀请码，选择 Basic/Plus/Max、兑换后天数、兑换截止和备注。用户在 Menu 兑换后立即看到真实档位、额度与到期时间；有效期内不叠加，到期自动回 Free，随后可兑换新码。数据库只保存哈希和末四位提示，明文只在创建时显示一次。
- 邮箱 OTP 仅保留兼容。若未来公开，必须先配置自定义 SMTP 和含 `{{ .Token }}` 的模板。
- 游客文章查词、独立查词和两类导入使用四个独立日额度；缓存命中、失败、超时和及时取消不扣。生产生效以最新 SQL 迁移验收为准。
- 保存、生词/Anki、私有全文翻译和摘要需要登录；管理员预发布缓存可公开。
- 云端为跨设备权威，本地仍是工作副本和离线层。协议 2 先做 bounded snapshot，再用 opaque `(updated_at, kind, object_key)` cursor、version/hash manifest 和 CAS 合并。
- 本地修改约 800ms 后上传；可见页约 15 秒拉取并在 focus/visibility 返回时检查。不能恢复成每次下载完整历史。
- 离线模式只开放上次验证账号在当前浏览器的文章、生词、阅读状态和缓存，始终明确提示；不恢复 Admin、套餐、额度或 API 权限。

## 10. 推荐文章与 Admin

- 推荐摘要必须 server render，不能只在 client mount 后 fetch。
- 候选来源为浏览器保存文章、粘贴、URL 和代码白名单 RSS/Atom。URL 与 crawler 共用 `/api/import-url` 和正文边界清理；crawler 永不自动发布。
- 候选和已发布文章都打开真实 `ReaderView`；正文编辑回写当前 row，标题、摘要、分类和封面由 Admin 管理。
- 推荐封面是发布必需项。外部候选封面与正文图片在发布前经固定 DNS 安全抓取、类型/大小校验、Sharp 转为有界 WebP，并存到 `public-article-covers`；公开 Reader 不依赖访客直连来源 CDN。旧公开文章可通过受保护的图片修复动作补齐。
- 只有服务端验证的 `admin` entitlement 是正常入口和权限边界；旧密码 Admin 只是恢复入口。
- Admin 还管理账号/额度、私有反馈和详细错误记录。普通用户不看到 raw metric、内部安全额度或未启用价格实验。
- 文章、缓存、翻译、import 或 schema 变化必须同时检查 Admin 发布与公开缓存回放。

## 11. OCR、错误与安全

- OCR 不是当前用户功能。当前页面、帮助、Menu、额度和离线文案都不提供 OCR 入口或承诺。
- OCR provider、路由和旧 metadata 为兼容保留；`ArticleInput` 和 `ReaderView` gate 关闭，URL 图片不自动 OCR。
- 用户错误要区分输入不支持、断网、账号/额度和站点/上游故障；不能显示 `Failed to fetch`。
- 站点/上游/配置/客户端处理错误使用可追踪 id，先写私有存储，再可选发邮件；断网和输入错误不存为 bug。
- 所有 API 维持请求体上限、成本限流、并发闸门、同源 Admin 写入、DNS pin、私网阻断和通用外部错误。浏览器没有 Supabase key，RLS/权限撤销必须真实执行。

## 12. 模型与重要路由

DeepSeek 路由默认 `deepseek-v4-pro` 且关闭 thinking。`DEEPSEEK_MODEL` 为共享覆盖，`DEEPSEEK_TRANSLATION_MODEL` 只覆盖全文翻译，`DEEPSEEK_FALLBACK_*` 配置可选回退。

关键路由：

- `/api/explain-word*`：文章语境解释
- `/api/dictionary*`：独立双向词典
- `/api/translate-article`：全文翻译
- `/api/import-url`：安全 URL 导入
- `/api/pronunciation`：统一云端发音
- `/api/public-articles*`：公开推荐
- `/api/auth/*`、`/api/account/*`、`/api/usage/*`：账号、同步、用量
- `/api/admin/*`、`/api/cron/recommendations`：推荐与运营
- `/api/anki/*`：Anki 辅助，实际导入仍依赖浏览器访问本地 AnkiConnect

## 13. 生产与文档

常规用户可见代码变更先运行：

```powershell
npm.cmd run build
```

然后使用 `ops/mainland/` 累计版本化发布。并行任务各用独立 `codex/*` 分支与 worktree，生产候选只能来自当前接受版本之上的干净集成提交；清单记录父版本、源码提交和精确文件差异。服务器固定发布入口以全局锁、两次父版本检查和永久行为契约拒绝并发覆盖或陈旧候选，只重建 `app` 与 `caddy`，不重启数据库/Auth/REST/Storage/网关。只有公网 `/api/connectivity` 返回精确的新旧版本身份且相关回归通过，才能说已经上线。完整规则见 `docs/release-governance.md`。Vercel 不做日常发布。

文档职责：

- `AGENTS.md`：下一位编码 Agent 必须遵守的规则
- `PRODUCT.md`：产品原则
- `docs/architecture.md`：实现细节
- `docs/integration-guide.md`：环境/API/集成
- `docs/release-governance.md`：并行开发、累计打包、生产门禁与恢复
- `docs/home-v2-implementation-contract.md`：主页当前状态、下一方向、验收方法
- `docs/product-journey.md`：UI、功能、基础设施和关键决策的时间线

最终状态写回对应文档；历史只追加到 `docs/product-journey.md`，避免上下文不断膨胀。

## 14. 已知未来候选

用户曾提出基于查词习惯形成动态阅读词汇画像。可以记录每千词查词密度、词频等级、重复查词、保存率、单词/短语比例和主题差异，输出舒适区与趋势；没有校准测试时不能宣称精确词汇量。

## 15. 回答方式

- 用中文，先给结论。
- 清楚标注观察、推断、已实现和待决定。
- 不编造本地代码、生产状态或视觉验证。
- 开放 UI/功能设计先问清目标并共同收敛，不直接大规模实现。
- 讨论任何功能时同步考虑 Reader、Admin、缓存/同步、移动端、离线与 Anki 的影响。

## 2026-08-17 首页视觉纠偏最新覆盖

- 用户看过真实稿后否定第十八轮登录工作台构图：继续阅读缩到一半以内，导入器更大，同一首屏居中；最近文章删除，重复说明文案删除，输入聚焦与正文/网址切换不得改变外壳尺寸。
- 第 22 轮追加纠偏要求封面到外刊的约 `1.9s` 双向吸附使用全程同一空间速度，不能明显加速减速；球体分向离场与外刊落位共用终点。Ballpit 从实时位置逐球让路，静置时通过质心回归与随机软包络压力让框外只剩少量离群球，但仍无固定归位点。
- 2026-08-18 用户在第 21 轮报告静置约 `6–7s` 仍会卡。除高刷 Ballpit 和字母空转优化外，同机对照进一步确认 3000 开发模式存在约 `519–620ms` 周期性停顿，3100 生产构建 15 秒最差约 `16.8–25.4ms`。以后只用 3100 判断视觉流畅度，但用户原设备的生产体感仍是最终门槛。
- 三张超大功能卡已被覆盖。七张较大卡使用连续浮点轨道、不可见区循环和按距离固定的 z 层级；拖拽最多跨两张并保留轨迹，点击沿最近方向居中，前方约三张可读。该交互重构已接入，真实手感、最终尺寸和手机方案仍待验收。
- 2026-08-21 最新覆盖：用户选择 `opening=wordfall` 并舍弃旧螺旋/球团/开孔；本地已按真实大小写词形重采样，能够读出 `Context Reader`，但仍是待用户设备视觉验收而非最终完成。
- 左上品牌与高频按钮贴近最左侧；高频按钮不能复制品牌/Menu 胶囊悬停。Menu 内容向左展开且必须不透明可读。主推外刊保留 3D，删除明显圆形光斑，并补 Lusion 式文字响应。
- 桥接句暂用“读完这些，也别忘了那篇一直没有读下去的文章。”；该标题已从约 100px 级缩到桌面最大约 52px。外刊图片进入采用与滚动速度无关的约半秒清晰放大（`scale(.90) → scale(1)`），全程不模糊、不裁切铺开，慢速滚动也应清楚可见；共享图片转场仍未完成。联系区暂用“如果哪里还不够好，告诉我。”；第一屏副句未通过。

- 第 23 轮 309–340 与第 24 轮 341–343 已提交并归档。当前 Beta 暂不开放密码找回，等待合规短信验证；未来重置后回到登录框由用户明确登录，旧六位数字密码继续兼容到用户主动改密。昵称 + 未验证手机号不能作为所有权证明。
- 新注册只收手机号标识、昵称与密码，不再收年龄、性别、英语水平和阅读偏好。新密码为 8–72 位可打印字符且至少含字母和数字；旧六位数字密码暂保留登录兼容。账户页低调支持改昵称和改密，手机号只读。
- Reader 桌面工具与操作条固定锚定视口左侧，正文和解释栏按剩余宽度伸缩；独立查词窗可部分拖出左右屏幕但必须留出可抓回区域。文章保存后按钮禁用显示“已保存”，不再提供重新生成首页摘要。
- 外刊推荐封面只用于橱窗和进入转场，不再额外注入正文；Reader 保留原文章图片，避免首图重复。外刊返回恢复原卡、分类与滚动；自主导入草稿保留；会话栈最多 5 篇。
- Menu 设置现有字母轨迹、全站日夜主题和外刊图片 3D。手机生词详情及其他悬停内容必须可点按。游客封面到外刊使用更快但仍线性的同一时间线；游客额度用完后，再次点击重试控件只打开登录，不显示原始限流错误。
- Ballpit 仍是无固定单球目标的热运动；2026-08-22 将群体质心与软包络回流提高约两倍，使静置时右侧宏观活动区外通常只剩少数离群球，鼠标仍可临时撞散。
- 当前根首页已部署为大陆生产评审基线 `20260821T174458`（父版本 `20260821T172903`，源码 `9ad8839ef8f877ee31070ea60a8bb3da8c22f48f`；首页视觉代码源自直接父版本 `22c8af48a2a58b11f723bbcb4c506666e498733b`），旧 `/home-v2` 只做兼容重定向；球体透明/强化群体回聚、线性双向吸附、约半秒整体缩放的外刊重播、Menu 设置、查词重排与清晰粒子拼字均已上线，但复杂视觉仍需用户关键帧与重复使用验收。
