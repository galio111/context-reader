# Context Reader 首页重设计当前决策总表

更新日期：2026-08-18

这份文件只保存当前有效结论，是首页重构开始前的第一读取入口。它不保存完整历史，也不把问卷草图当成最终 UI。详细交互和验收规则继续以 `docs/home-v2-implementation-contract.md` 与 `docs/lusion-recommendation-motion-spec.md` 为准。

## 如何解决冲突

1. 当前代码事实以实际代码和 `docs/architecture.md` 为准。
2. 待实现的产品目标以本文件中的“已确认”条目为准。
3. 同一问题出现多次答案时，用户最后一次明确纠偏覆盖旧答案。
4. `docs/product-journey.md` 与 `docs/home-redesign-interview-archive.md` 可以保留旧决定作为历史证据，但旧决定不能覆盖本文件。
5. 问卷里的颜色、字号、比例、圆角、草图和候选文案只有在用户明确接受视觉或文案后才能进入当前决定。
6. 标为“暂定”或“待确认”的内容不得在实现中擅自固化。

## 状态词

- **已实现：** 当前代码已经运行。
- **已确认，待实现：** 产品方向已定，但代码尚未完成。
- **暂定，待体验：** 只能通过真实原型继续判断。
- **待确认：** 仍在访谈中，不能自行选择。
- **已覆盖：** 历史决定，禁止作为当前实现依据。

## 当前实现与实施边界

- **已上线：** 裸域名 `https://context-reader.com` 是唯一规范首页，地址栏不再保留 `/home-v2`。根路由直接服务 `HomeRedesign` 与 SSR 外刊；旧 `/home-v2` 只作为兼容入口永久跳回根首页并保留查询参数。所有站内返回首页、退出登录、PWA 启动和离线回退都指向根首页。旧 `BookHome`、`CurvedPageTurn` 仍留在代码中作历史兼容，但不再是首页渲染基线。
- **已实现，待视觉验收：** 当前工作区已移除真实首页的书本式翻页和滚轮翻页，接入 Ballpit 品牌封面、真实 SSR 外刊、自主导入、功能介绍与联系区；这些只是持续迭代中的真实切片，不等于 Lusion、球幕和全站视觉已经通过。
- **已实现，待用户设备复验：** 用户在第 21 轮再次指出游客与登录视角均明显卡顿，游客更严重，并具体观察到静置约 `6–7s` 就停顿一下。现场诊断发现高刷新率屏幕使 Ballpit 跟随约 `150–165Hz` 持续更新，同时鼠标字母画布在无交互时仍持续申请动画帧；游客首屏会叠加两套全屏画布。当前本地实现已把 Ballpit 更新上限压到高刷屏约 `82Hz`、普通屏保持原生约 `60Hz`，降低球体几何/像素预算与数量，缓存指针边界，并把字母场改为只在初绘、指针移动和粒子存续时运行。进一步对照确认 3000 开发模式存在约 `519–620ms` 的周期性停顿，而相同页面的 3100 生产构建 15 秒内最差帧间隔约 `16.8–25.4ms`；后续复杂动效只在生产构建验收。该证据仍不能替代用户在原设备确认 3100 体感。
- **已确认，待实现：** 不创建 `/home-lab` 或断开真实功能的假首页。第一版可评审切片就必须连接服务端推荐、`HomeClient`、账户/Menu 和真实 `ReaderView`。
- **已确认，待实现：** 技术通过、视觉还原、重复使用体验是三道独立验收；只有用户明确接受，复杂动效才能成为受保护行为。

## 产品定位与首页顺序

- **已确认：** 产品初心是让用户遇到长篇或难读英文时，可以粘贴正文或输入文章网址，迅速进入适合阅读的正文，并在原文语境中理解词语。
- **已确认：** 语境解释与语境翻译仍是核心能力；精选外刊与自主导入是并重的两条入口。
- **已确认：** 新用户与未登录用户的首页顺序为：品牌封面 → 精选外刊 → 自主导入 → 功能介绍 → 联系与意见反馈。
- **已确认，覆盖第 18、19 轮：** 登录用户首屏把继续阅读与自主导入放在同一居中工作台：继续阅读进一步缩小，导入器成为更突出的主模块；不显示“接着上次停下的地方”“带一篇你的文章来”等可由界面直接理解的说明。最近文章暂时移除，只保留“我的文章”入口。随后进入精选外刊与可展开库存，最后保留联系与意见反馈。
- **已确认：** 有阅读记录时，“继续上次阅读”是第一主动作，自主导入紧随其后；无阅读记录时隐藏空的继续阅读，把自主导入和精选外刊前置。
- **已确认，覆盖单一编辑式方向：** 继续阅读默认使用克制封面参与版本，让文章更快被辨认；当文章没有封面图时自动回退到编辑式文字、进度和极短预览。模块已有悬停抬升、边界和箭头反馈且方向获接受；不附加最近文章列表，完整历史从“我的文章”进入。
- **已确认，按最新工作台纠偏：** 手机保持“继续阅读 → 自主导入 → 外刊”的信息顺序，不重复最近文章列表，也不要求继续阅读与导入器强行塞进同一屏；完整历史仍从“我的文章”进入，正常纵向滚动优先。
- **已确认：** 完全没有阅读记录的登录用户不显示空继续阅读或空最近文章；导入器成为第一主景，精选外刊紧随其后。

## 首页后半程屏间连续性

- **已确认，覆盖范围仅限封面之后：** 外刊、导入、功能卡片与联系区都使用浏览器自然滚动；动效只读取区段进入与离开的进度，不吸附、不分页、不接管持续滚轮。封面到外刊之间仍按下文保留一次轻度 `proximity` 吸附试验，二者不是同一范围。
- **已确认，待实现/验收：** 外刊最后一行正常滚出，同一浅色纸面缓慢换色，以桥接句进入个人阅读现场；不使用硬切背景或另一块突然盖上的整屏容器。
- **已确认，待实现/验收：** 导入器进入视口时按“模式标签 → 输入面 → 动作”分层落位；离开时继续与功能介绍形成视觉接力。第 19 轮已否定三张超大功能章节作为当前方向。
- **已确认方向，待视觉验收：** 功能能力改为七个较大的功能卡围成立体环形。前方同时可辨识约三张，其余形成空间背景；鼠标拖拽、触摸滑动、左右按钮和点击卡片都能旋转到目标卡，快速拖动最多跨一至两张并保留连续轨迹，左右无限循环且不显示 `1/2/3/4` 序号。每张卡使用真实小画面、二至四字名称和一句短说明，并有明确悬停反馈。环形方向已接受，但卡片最终尺寸、真实画面、透视和手机降级仍须真实验收。
- **已确认，待实现/验收：** 手机保留相同顺序和关键接力，减少重叠和运动距离并允许自然滚过；系统减少动态效果时保留色场和布局接续，取消覆盖、视差和大位移。

## 登录状态与当前旅程连续性

- **已实现，待完整旅程复验：** 首页首次完成账号状态解析后锁定本次旅程外壳。游客在当前首页中途登录只更新身份、权限和同步，不切换到登录工作台；下次全新打开才默认进入登录工作台。Reader 中途登录与全部来源返回仍按下条继续复验。
- **已确认，待实现：** 游客从某篇外刊进入 Reader、在 Reader 中因受限能力完成登录后，继续停留在当前文章；返回时至少必须恢复登录前的游客外刊界面、分类和滚动位置。此前“Reader 返回一律直达 Ballpit 封面”的规则不再足以覆盖该场景，完整返回矩阵需后续单独收口。
- **已实现，待浏览器登录实测：** 游客在首页 Menu 中登录后仍停留在当前游客首页和原位置。本次挂载不因身份变化自动换首页；下次全新打开网站时，再按登录身份进入工作台。
- **已实现：** 账号与数据页和 Menu 账户内容保留退出登录；退出后结束账号工作层并进入游客首页。
- **已覆盖：** 游客能力、四池额度、受限动作和登录续接已经在“游客权限、额度与登录续接”一节收口；此处不再作为待访谈项。剩余待定只包括提示的最终视觉与文案。

## 审美与表达基线

- **已确认：** 用户喜欢现有五彩 Ballpit 球体碰撞，把它视为封面或品牌 IP；喜欢鼠标移动时字母飘出的效果；喜欢现有封面按钮的悬停反馈。
- **已确认：** 开发者的话真诚，适合重构后进入使用说明，而不是丢失或继续作为主页长篇介绍。
- **已确认：** 用户喜欢 I Read World 的简洁、清晰和外刊内容秩序，但明确不要求模仿其视觉；Context Reader 必须保留自己的品牌第一印象。
- **已确认：** Lusion 录屏定义精选外刊的悬停 3D、滚动出现和点击共享图片转场目标；复刻的是动效语言，不是其品牌、内容或私有源码。
- **已确认：** 首页可以表现力强，但不能“已经花哨到影响使用，却仍静态、平庸或不惊艳”。强动效要集中在少数关键时刻，Reader 保持安静。
- **已确认：** Menu、浮层和悬停内容不能再用透明背景牺牲文字可读性；可读性不是后续美化项。
- **已确认：** 文案不能直白粗糙，也不能为了美感变得看不懂。除已确认主句外，候选文案必须继续由用户把关。
- **已确认：** 用户不接受“AI 做完一整套再开盲盒式调 UI”。复杂界面和动效必须小切片、真实功能接线、浏览器自检和用户逐段验收。

## 封面与第一屏

- **已确认，待实现：** 保留现有 Ballpit 五彩球体自由碰撞效果，把它作为品牌/IP 门面；不再暗示实体书。
- **已确认，待实现：** 新用户封面约占一屏的大部分高度，精选外刊从底部露出一线。
- **已确认，待实现：** 静态构图是有留白的品牌门面、无明显边框的中心球场和偏左克制文案；不是全屏球幕，也不是常规左右分栏 Hero。
- **已确认：** 第一屏主句是“在语境里，读懂英文。”，信息密度为主句、一句克制说明和一个进入动作。
- **待确认：** 副文案、动作名称、字宽、断行以及封面底色。第 20 轮三句副文案全部被否决，而且用户尚不清楚副句会出现在哪个具体位置；下一轮必须先用真实位置或示意图说明，再讨论文字，不能脱离构图继续给句子。

## Ballpit 到精选外刊的转场

- **已确认，待实现：** 封面等待时，球体一直自由漂移和碰撞。
- **最新试验，覆盖“不吸附”的旧结论：** 游客封面到外刊仍由浏览器自然滚动进度驱动，不拦截滚轮，也不恢复分页；但第 21 轮要求加入轻度 `proximity` 自动吸附真实体验。向下与向上必须使用同一连续时间线正放/倒放，不允许强制锁滚或跨段跳转。第 22 轮根据真实手感决定保留、减弱或删除吸附。
- **已确认，待实现：** 转场必须从触发瞬间每颗球的真实位置和速度继续，不能先换球阵、重置坐标、切画布或切到预设场景。
- **已确认，本地精修待验收：** 转场使用同一个覆盖视口的 Ballpit 画布，不允许球群连同旧容器边界一起移动。球在到达真实视口边缘前必须完整可见，可以飞越下一屏的“精选外刊”文字；中途停留不得闪现另一颗球、切画布或重新随机。球体先让出中央通道，接近终点才完全离开；外刊列表同时从下方连续上移并承接 Lusion 式速度形变。
- **已确认，本地已接入待验收：** 列表完全到位的同一终点，球体画布必须彻底隐藏，后续页面边缘不残留球；反向滚动到转场时再恢复同一画布。资源是否在终点直接卸载仍需与可逆滚动和重建闪烁共同评估。
- **已确认的性能红线：** 普通滚轮、任意中途停留和封面静置都不能出现闪屏、跳到更下方滚动位置或可感知卡顿。实现必须避免每帧布局测量和重复初始化 WebGL；低性能设备的实际降级阈值仍需下一轮真实体验确认。构建通过或高性能开发机流畅不能替代该验收。
- **已确认：** 封面、外刊、导入、功能环和封底保留不同的低饱和色场，但相邻边缘必须以长距离渐变和共享色彩线索柔和融合，不能出现整屏硬切或明显色带。
- **已确认，待实现：** 转场未结束时明确向上滚动，从当前进度自然反向；完成后轻微反向滚动不重播开场。
- **暂定，待体验：** 点击主动作的平滑滚动以约 1.4–1.8 秒观感为首轮目标；手动滚动则完全跟随真实滚动进度，不强行套固定时长。
- **已确认：** 点击主动作使用同一时间线的稍快版本；手动滚动仍然可逆，点击不能引入另一套球路径。
- **已确认：** 转场完成后的滚轮只负责正常页面滚动，不再翻页、吸附或争夺页内滚动。

## 精选外刊布局与内容

- **已确认，待实现：** 桌面游客橱窗约两屏、约 10 篇：第一篇主推独占全宽首行，随后 3 行每行 3 篇。标准行一旦出现就必须满 3 篇，不能留下单篇残行。
- **已确认，待实现：** 手机端主推全宽，其余文章两列，展示数量可以比桌面少。
- **已确认，待实现：** 图片使用约 4:3 的正常文章比例并保留页面侧边空间，明显小于 Lusion 录屏，但不降低 3D 悬停和共享图片转场的目标强度。
- **已确认：** 卡片默认信息是标题、刊物来源、难度和阅读时长，不常驻摘要。
- **已确认：** 分类位于标题下方，默认先显示“推荐”，之后是时事、科技、文化、商业等；各分类首页数量大致相同。
- **已确认，待实现：** 分类切换保持网格槽位和页面高度基本稳定，旧内容从原位置退出，新内容在相同位置接续并带轻量速度形变；不是瀑布流或追加。
- **暂定，待体验：** 分类切换、滚动出现、3D 悬停和点击转场的最终视觉只以真实浏览器效果为准。
- **已确认，覆盖第 19 轮标题联动：** 外刊图片保留 Lusion 强度的 3D 倾斜；删除跟随鼠标的圆形聚光/球形光斑。标题、来源和元数据悬停时都不能改变行数、字距或卡片构图，文字保持原位，只在右上角显现进入箭头。
- **已确认，本地继续精修但未达视觉门槛：** 外刊标题、分类与首批图片进入必须复刻用户录屏中的图片纵向滞后、轻微倾斜、拉伸、速度模糊和停止回弹，不能退化为普通 `opacity + translateY`。当前本地增加遮罩揭示、图片/文字分层与速度变量，只是更接近参考的可评审切片；未完成共享 WebGL 表面与逐帧并排前不得称为复刻完成。

## 游客橱窗、登录用户库存与 Admin

- **已确认：** 游客只看有限的首页橱窗，不获得“显示更多”入口。
- **已确认：** 登录老用户先进入工作台；当其滚动到首页最后的外刊段时，先看到与游客同样的有限橱窗，再通过“显示更多”把其余外刊直接展开在这段下方。
- **已确认：** 展开区不跳独立路由、不打开覆盖层、不新增“完整外刊库”标题；它是登录用户首页最后一段，页面随库存变长可以接受。
- **已确认：** 登录用户展开区首版提供分类、难度与搜索。
- **已上线，待内容验收：** 首页推荐位置与文章发布状态分离。发布文章只进入外刊库；只有被编排进槽位的文章进入游客橱窗。空编排时保留旧顺序作为迁移回退。大陆生产 `20260820T230128` 的公开接口返回 13 篇真实文章。
- **已上线，待 active Admin 实测：** Admin 已增加“首页外刊编排”，使用推荐、时事、科技、文化、商业分类标签页与可拖拽/键盘移动槽位；推荐首槽标记为主推。未授权公网请求已验证为 401，仍需使用真实 active admin 验证保存与 SSR 回放。

## Lusion 点击转场、Reader 封面与返回

- **已确认，待实现：** 点击外刊图片直接启动共享图像转场并进入真实 `ReaderView`，手机端也不增加二次确认。
- **已确认，待实现：** 色场从对应封面自动提取低饱和主色，并为过亮、过暗或不适合的颜色提供品牌回退。
- **已确认，待实现：** 封面最终落入文章顶部，成为文章真实、持久的头图；不自动淡出、缩走或卸载。读者向下阅读时，它只随正文自然滚出视口，回到文章顶部仍能看到。
- **已确认，待实现：** Reader 不显示 `Context Reader` 品牌胶囊，只保留当前网站布局中原有位置的返回按钮，不重新挪位。文章进入 Reader 的正向共享图片转场仍保留。
- **待确认，覆盖上一轮的绝对终点：** 返回不能再被实现成无条件直达 Ballpit。至少在游客浏览或 Reader 中途登录的旅程里，返回要恢复进入文章前的游客外刊位置；其他来源的返回目标、是否使用反向共享图片和“回到首页”标签是否仍准确，需按来源矩阵重新确认。

## 左侧高频导航与右上 Menu

- **已确认，待视觉验收：** 未登录桌面首页仍由左上品牌胶囊悬停/聚焦唤出四个高频按钮，不出现整高栏面或侧栏背景；登录用户首页则与 Reader 一样固定显示四个按钮。品牌胶囊和按钮列都贴近视口最左侧留少量安全边距；四个高频按钮必须使用不同于品牌胶囊与 Menu 的外形和悬停语言。登录首页点击品牌胶囊返回该登录用户本次首页的起始状态，而不是切换到游客封面。
- **已确认：** 四个按钮使用简洁小图标 + 二至四字短标签，悬停/聚焦才显示完整说明；保留当前网站按钮的出现动效和悬停反馈语言。图标、间距、方位和具体视觉仍需真实稿确认。
- **已确认的状态修正：** 游客点击任一高频按钮后，只打开对应真实功能；按钮组在指针离开或焦点转移后仍须按原规则收起，不能因为点击后的焦点状态永久固定。下一轮用“横移分层、底色填充、仅图标微动”三种真实微动效比较最终悬停语言。
- **已确认，本地已接入待验收：** Reader 中四个高频按钮独立固定显示，不依赖品牌胶囊或悬停出现。返回首页与四个按钮在视口最左侧留少量边距形成同一纵列；查词小窗默认停在该列右侧，正文与右栏整体右移以避免遮挡。真实导入工作层、保存文章层、查词窗口和生词本入口已连接；精确间距、窄桌面降级和视觉仍需真实稿。
- **已确认，覆盖动态标签候选：** Reader 左上按钮始终显示“返回首页”，外观与位置保持当前方向；实际行为仍按进入来源恢复工作台、工作层或外刊状态，因此无障碍名称与辅助说明必须让返回目标可理解。
- **已确认，待实现：** 从“继续上次阅读”进入时恢复该文章上次的真实阅读位置，返回则恢复登录工作台原位置并让继续阅读模块可见；首页不再提供最近文章模块；从“我的文章”进入时重新打开同一工作层并恢复搜索、筛选、滚动与被点击文章；从生词本原句进入时恢复生词本工作层、搜索、滚动与原词条选择。
- **已确认，待实现：** 粘贴正文进入 Reader 后返回正文模式，但输入框清空；URL 导入进入后返回 URL 模式并保留原网址与已确认预览。Reader 内继续导入新文章形成当前标签页内最多 5 篇的会话栈，第一次返回先恢复上一篇 Reader 的正文、阅读位置和面板状态，栈用尽后才恢复最初来源；浏览器后退、鼠标侧键、手机系统返回与页面按钮使用同一顺序。
- **已确认，待实现：** Reader 刷新直接回首页；已保存文章的进度继续用于“继续阅读”。登录用户的未保存粘贴文章建立一个仅存于当前浏览器的临时阅读槽位，明确标记“未保存”，下一篇未保存文章会替换上一条；游客不建立该临时记录。Reader 中主动退出登录不构成例外，结束 Reader 并显示游客首页，同时保留允许保留的本地阅读进度，清除账号专属工作层与临时未保存入口。
- **已确认，待实现：** 外刊返回时如果原卡片已下架、换分类或列表更新，先用进入时快照完成反向转场，再同步最新列表；失效卡片随后平静淡出并说明内容变化。游客反向共享转场目标约 `1.8–2.0s`，登录用户约 `0.9–1.2s`，压缩版仍须真实关键帧验收。
- **已确认的转场分工：** 只有从外刊卡片进入 Reader 使用 Lusion 共享图片转场；继续阅读、导入、生词本等没有真实原卡片的来源使用约 `180–260ms` 的克制空间回落或交叉淡化，不伪造卡片。外刊卡片图、转场层和 Reader 顶部封面必须表现为同一视觉对象的连续形变：原卡片位置只保留无图占位，转场层最终成为 Reader 顶部封面，不同时显示两张相同图片。
- **已确认的加载协调：** 点击外刊立即并行请求文章与启动共享转场，不再叠加现有通用 Reader 加载动画。数据在转场结束前到达时，封面、标题和正文随同一次转场落位；若数据更慢，已经落位的 Reader 封面保持稳定，只在其下方显示正文骨架，不能再切到全屏加载画面。登录用户全新打开网站的 Ballpit 开场与文章共享转场互斥：前者只属于站点启动，后者只属于外刊点击。
- **已确认，本地已接入待验收：** “单独查词”只从桌面固定/浮出高频按钮或手机底部 Menu 打开，不在老用户第一段重复输入框，也不再在 Reader 工具栏保留另一套单独查词入口。桌面已复用完整 `BookDictionary` 单实例窗口，可移动、缩放、隐藏并与首页共用位置记忆；不另做缩水词典，也不允许同时打开多个词典窗口。
- **已确认，待真实布局验收：** Reader 宽屏默认位置不是右侧浮窗。固定高频按钮位于左侧，查词小窗紧邻按钮停在左侧留白；正文与右侧解释面板作为一个整体向右偏移到足以容纳该工具区，默认不遮挡文章。用户提供的 `1919×1029` Reader 截图只用于说明空间关系，不冻结偏移像素。正文宽度不得为给小窗让路而任意缩窄；具体小窗初始宽高、工作区偏移公式、最小右侧安全边距和窄桌面降级阈值需在真实 Reader 多视口原型中计算。
- **已确认，待实现：** 窗口外壳固定，输入与关键操作固定在顶部，长词条结果区独立滚动且不把滚轮传给文章；克制的顶部标题栏是明确拖动区。窗口可自由拖动，接近视口边缘时轻微吸附并始终保留完整标题栏；右下角可自由缩放。最后位置与尺寸在同一浏览器长期保存，分辨率或可用视口变化后自动校正回可见区域。
- **已确认，待动效验收：** 点击关闭后窗口完全隐藏，不留下第二个悬浮气泡或边缘标签；关闭动效朝固定“单独查词”按钮收束，形成窗口被吸回按钮的空间联系。具体轨迹、遮罩、时长和材质待真实动效比较，系统减少动态时改为短淡出。隐藏后重新打开保留查询与结果，但结果滚动回顶部并聚焦输入框。
- **已确认，待实现：** 切换首页、Reader 或打开另一篇文章时，词典窗口自动隐藏但保留全部查询状态。再次点击高频按钮时，隐藏则打开；已经打开则只提到最前并聚焦输入框，不切换隐藏、不清空。剩余 3、1、0 次的独立查词额度放在结果区顶部状态行，与“词条已生成”同层；用完时同一位置提供登录动作。
- **已确认，待实现：** 手机不使用自由拖动窗口，从底部 Menu 打开约 `85–90vh` 的底部面板；输入区固定，结果内部滚动，关闭后再次打开可恢复同一查询状态。最终面板视觉、关闭手势和键盘顶起后的高度处理仍需真实设备验收。
- **已确认：** 四个入口点击时从按钮位置发起同一段短过渡，但按任务落入不同工作表面；统一的是动作来源与节奏，不是强迫所有任务使用同一容器。
- **已确认：** 手机品牌胶囊保持单行并缩短字号和留白；两行按钮已否决。
- **已确认，覆盖旧决定：** 手机端不再设置左边缘把手、左栏或左右双入口；四个高频入口与低频内容统一集成到底部 Menu。桌面右上 Menu 不重复四个高频入口。
- **已确认，覆盖旧决定：** 首版 Menu 不提供“设置与无障碍”。桌面目标 Menu 只保留登录/账户与用量、使用说明、意见反馈，以及服务端授权后才出现的 Admin。系统级键盘、焦点、语义、触屏替代、可读对比和跟随系统减少动态仍是实现底线，不需要用户打开设置。
- **已确认：** 桌面 Menu 由右上按钮打开紧凑浮层，不做第二条整高栏。悬停只高亮项目；点击后不跳转新页面，详情在同一 Menu 浮层关系中替换或向左展开一块可承载内容的区域。用户把这块内容区视为 Menu 自己的“页面”。
- **已确认：** Menu 顶部先显示登录或当前账户身份，随后为使用说明、意见反馈，服务端授权 Admin 固定最后。
- **已确认：** Menu 表面接近不透明，只保留非常轻的环境透色；清晰可读是硬底线。点击使用说明、账户或反馈后，内容区从第一层左侧扩展，右侧 Menu 列表保留。点外部、Esc 和关闭按钮均可关闭；表单未提交状态的防误关仍需实现时处理。
- **已确认：** 鼠标移动时彩色字母蹦出效果继续覆盖非 Reader 的游客首页与登录首页；进入正式 Reader 后不出现。
- **待真实稿确认：** 桌面 Menu 第一层的精确宽高必须先做真实页面稿，不能靠文字选择。手机底部 Menu 默认约 `58–65%` 高，内容不足时不强撑满，支持下滑、点遮罩和关闭按钮关闭；其顶部装饰层必须以不同低饱和颜色和可辨识的高度错位同时露出，不能最终只看到一张同色卡。键盘顶起与未提交表单仍需真实设备验证。

- **已确认：** 未登录桌面首页悬停/聚焦品牌胶囊后，四个独立小胶囊从下方向下纵向浮出；每个为简洁图标 + 二至四字。登录用户首页同样固定在品牌胶囊下方沿左侧排列。手机不显示这列，统一进入底部 Menu。
- **已确认：** Menu 第一层未登录显示登录动作；已登录只显示昵称与同步状态，额度详情进入账户内容区，不把所有用量挤在第一层。

## 账户与使用说明

- **已确认：** 点击账户后先显示身份、登录状态与同步状态；额度、个人数据导出和退出按清晰分组继续向下。未登录用户点击登录时继续使用现有独立登录对话框，Menu 暂退到背景，不把注册登录硬塞进 Menu 内容区。
- **已确认，待实现：** 账户首版支持修改昵称与六位数字密码，手机号标识只读、暂不允许自行修改。修改密码必须保留账号安全、会话与同步数据边界，不能因修改凭据丢失本地或云端内容。
- **已实现，待真实账号与生产迁移复验：** 注册基础账号成功后在同一对话框进入可跳过第二步，可选填出生年份、性别、阅读偏好和英语水平；填过的内容在账户详情可见，跳过不阻断注册。
- **已确认：** 精选外刊推荐只使用英语水平和阅读偏好辅助排序；出生年份与性别只用于匿名汇总了解用户群体，不参与个人推荐。
- **已上线，待真实账号复验：** 只收出生年份，不收月日；性别只提供男、女且可跳过。两项不进入推荐排序，也不向其他用户展示；账户详情允许逐项清除。生产列迁移已于 2026-08-20 在可恢复备份之后完成。
- **知识纠偏与后题覆盖：** 出生年份、性别在与账号关联收集时仍属于个人信息；只有完成不可逆匿名化后的统计结果不再属于个人信息。第 82 题已经覆盖“无需说明”的同题补充：字段旁需以短句说明用途，使用说明保留完整章节，导出包含原始资料，账户详情允许逐项清除。
- **已实现，映射待内容样本调优：** 阅读偏好多选已使用科技与科学、自然与环境、文化与历史、社会与时事、商业与经济、人物与成长、故事与文学、健康与生活八项；推荐页优先使用文章结构化分类，缺失时使用克制的标题/来源启发式，后续需随真实外刊样本调整。
- **已确认，覆盖问卷选项文字：** 英语水平为单选：高中、四级、六级、考研、雅思/托福。不使用入门、基础、中级、进阶或 CEFR 作为用户选项。如何映射外刊难度仍待确认，不得自行按考试分数精确换算。
- **已确认：** 选填资料与手机号标识、昵称和六位数字密码位于同一注册表单下方，并明确标记选填；跳过后不自动提醒，只在账户详情保留完善阅读偏好的入口。
- **已确认：** 出生年份、性别和英语水平首版在账户详情中只读，但允许逐项清除；后续编辑能力再立项。阅读偏好是明确例外，可通过外刊区的个性化推荐控件随时修改。

## 个性化推荐控制

- **已实现，待视觉验收：** 外刊标题区已长期显示独立“个性化推荐”控制，游客、登录工作台和移动端均可打开；它与推荐、时事、科技、文化、商业分类同时存在，不取代分类。
- **已实现：** 个性化选择只重排默认“推荐”页；用户切换其他分类时仍按主动分类筛选，不被偏好强制改写。
- **已实现，待跨设备实测：** 游客选择保存在当前浏览器；登录账号选择通过协议 2 的 `preferences` 对象同步。游客中途登录时本页继续使用游客选择，不静默覆盖账号；账号云端偏好作为以后新打开网站的默认。
- **已确认：** 控件附近提示推荐外刊会定期更新；不把截图中的“每日更新”直接写成产品承诺，更新频率、更新时间和空档期文案仍待运营能力确认。

## 游客权限、额度与登录续接

- **已上线并完成基础接口验收：** 游客文章划词/短语 10 次、单独查词 5 次、正文导入 2 次、URL 导入 2 次按北京时间自然日分池，缓存命中、失败与及时取消不扣；保存文章、生词本/Anki、私有全文翻译与摘要要求登录。生产 SQL 已于 2026-08-20 应用，未登录会话接口已返回四池的正确额度；缓存退款、北京时间重置和并发幂等仍需行为级测试。
- **已确认，待实现：** 游客无需登录即可浏览并打开精选外刊、粘贴英文正文、输入公开文章网址和使用单独查词。图片、PDF 与 OCR 不在当前游客入口范围。
- **已上线，待完整行为验收：** 文章中的单词/短语查询每天 10 次；单独查词每天 5 次，两个额度池独立。两类查询命中已有缓存、失败或及时取消都不消耗额度；正文导入与 URL 导入各 2 次，四项可由 Admin 分别修改。
- **已确认，覆盖旧额度：** 游客每天可成功粘贴正文进入 Reader 2 次，可成功导入公开网址进入 Reader 2 次。无效输入、抓取失败、用户取消或站点故障不扣次数；同一已缓存网址再次成功打开不扣次数。文章查询、单独查词、正文导入和网址导入的游客额度都必须能由 Admin 调整，代码不得把问卷数字散落硬编码到多个客户端入口。
- **已确认：** Admin 分别配置文章查询、单独查词、正文导入、网址导入四项游客额度。保存后立即影响后续新请求，不重置当前已使用次数；首版不做可视化历史，只写服务端日志。
- **已确认：** 游客只查看公开推荐中已由管理员发布的全文翻译与摘要；自己导入文章的私有全文翻译和摘要需要登录。
- **已确认，待实现：** 游客点击保存文章、加入生词本或 Anki 时打开登录对话框；登录成功后保持当前 Reader 与滚动位置，并自动完成刚才唯一一次待续接动作。待续接动作必须有一次性 id，不能因重试重复保存或重复生成。
- **已确认：** 平时不主动催登录。只有用户点击受限功能、对应额度用完，或从 Menu 主动选择登录时才显示登录对话框；Menu 可用很短的语义说明登录能获得更多能力，但“登录可享受更多权益”不是已通过正式文案。
- **已确认：** 查询额度只在剩余 3 次、1 次与用完时提示；每天北京时间 0 点重置。
- **推荐位置，待视觉确认：** 文章查询提示放在右侧解释面板当前结果附近；单独查词提示放在可移动词典窗口自己的结果区。不使用遮挡正文的全站 toast。用完时在原位置解释限制并打开登录框。
- **已确认：** 游客登录时若账号已有另一套个性化偏好，当前页面继续沿用本次游客选择，账号偏好成为以后重新打开网站时的默认；不能在登录瞬间重排当前推荐列表。游客选择不自动覆盖已有账号偏好。
- **已确认：** 使用说明先解释产品是什么和四步首次阅读，再通过目录进入划词演示、Anki、数据与常见问题。划词短动图或交互演示由用户主动播放。
- **已确认：** 开发者的话位于完整使用说明末尾，作为独立短笺；不署开发者姓名。公开联系方式统一为邮箱 `13874807542@163.com`、微信号 `LA060321o`；原文和排版仍需用户把关。
- **已确认：** 手机底部 Menu 第一层上方使用两列按钮承载输入文章、单独查词、生词本、我的文章；下方用普通列表放账户、使用说明、意见反馈和授权 Admin，不包含设置。

## 自主导入与功能介绍

- **已确认：** 支持粘贴正文和输入公开网址，成功后必须进入真实 Reader；自主导入不是外刊的附属入口。
- **已确认：** 外刊区结束后用一句克制的桥接语和明显留白承接，再出现自主导入器；桥接文案尚未通过。
- **已确认，覆盖智能单入口：** 自主导入采用上方模式按钮切换不同输入方式。首版至少包含粘贴正文和输入网址；正文可直接进入 Reader，网址抓取先预览再由用户确认。新用户与登录用户是否采用完全相同的模式切换器仍需对照真实当前输入区确认。
- **已确认：** 网址入口附近提供一个很小的问号说明抓取边界，并用克制的强调色提示“并非所有网页都支持”；说明必须短，且不能把不支持、网络失败和站点故障混为一谈。正式文案仍待把关。
- **已确认：** 新用户与登录老用户复用同一套导入器基础 UI；差异在页面顺序、上下文和返回路径，不另造一套“老用户窗口”。
- **已确认，待实现：** 导入器默认选中“粘贴文章”，与“输入网址”使用一行紧凑模式标签；手机在未来入口增加后允许横向滚动。正文与网址草稿分别保留，切换模式不清空另一份草稿。
- **已确认，覆盖旧扩展规则：** 正文输入区聚焦时不能突然变大、加粗外框或破坏工作台比例；粘贴正文与输入网址切换时外层尺寸保持稳定。Enter 保持正常换行，`Ctrl+Enter` / `Cmd+Enter` 提交开始阅读；明确按钮仍是主要动作。
- **已确认，待实现：** 粘贴正文保留原段落和明确空行，只去除首尾空白与可可靠识别的复制垃圾；没有独立标题时使用首个有效文本行作为标题，进入 Reader 后仍可直接编辑。
- **已确认，待实现：** URL 抓取成功先展示只读预览，包括标题、来源、封面、短摘录与字数，再由用户确认进入 Reader；正文编辑仍只发生在 Reader。URL 失败时保留原网址，解释当前网站更适合改用粘贴正文，并提供明确切换动作；正式文案待用户把关。
- **已确认，待实现：** URL 问号在桌面悬停/聚焦、手机点按后显示约三四行短说明。文件或网址服务端处理的完整隐私说明只进入使用说明，不在输入器旁常驻额外隐私句。
- **已确认，待实现：** 正文或 URL 进入 Reader 使用约 `180–260ms` 的克制回落；处理较慢时只在 Reader 内容区域显示骨架。提交期间按钮内部显示进度并锁定本次动作，重试必须幂等。游客因额度用完登录后保留草稿或预览，并自动恢复一次刚才的导入。
- **已确认，待规划：** 未来文件能力先做 PDF，首版只支持自身包含可提取文本的 PDF；检测到扫描版时明确说明暂不支持，不自动 OCR。独立图片入口、OCR 入口以及尚未完成的 PDF 按钮全部隐藏，不能显示“即将推出”占位。PDF 仍是未来立项，不代表本轮立即实施。
- **已确认，覆盖旧待定：** 当前图片/OCR 继续不是用户功能；未来 PDF 完成前，首页导入器只显示已经工作的粘贴正文与输入网址。
- **已确认，覆盖三张大卡：** 功能介绍位于自主导入之后、联系与反馈之前，覆盖语境解释、全文翻译与摘要、精选外刊、自主导入、继续阅读、生词与原句、Anki 与同步等能力；使用七张含真实小画面的卡片组成可双向无限循环的立体环。前方三张可读，点击卡片沿最近方向居中，鼠标拖拽/触摸滑动为主、左右按钮辅助；快速动作最多跨一至两张且必须保留可见轨迹。远端卡片只能在不可见区循环，z 层级按离中心距离固定，不能出现第三张压住第二张或拖后层级变化。卡片悬停采用轻抬升、边界变化和深度前移。以上交互已本地重构，真实手感仍待第 22 轮验收。
- **已确认：** 游客首页最后以安静、留白很多的杂志封底式联系区收束：微信号 `LA060321o` + 一键复制；桌面只有指针停在微信号文字本身时才在其右侧显示克制的小二维码浮图，空白区不得触发，手机则点按显隐。二维码不参与布局、不挤动反馈区。意见反馈在原位置平滑展开表单；随后是包含 ICP 备案 `蜀ICP备2026045148号-1`、公安备案 `川公网安备 51010702044616号`、版权、邮箱 `13874807542@163.com`、使用说明与意见反馈的短页脚，两项备案分别链接工信部与公安部官方查询入口。只表述产品仍在持续完善，并链接更新记录，不承诺固定频率。
- **已确认：** 第一屏副句走更有文学感但仍能理解的阅读表达；外刊到自主导入的桥接从“策展阅读”转向“个人阅读现场”。正式句子必须单独给用户把关。
- **已确认的复杂动效验收：** 首次评审提供参考录屏与实现录屏并排，并列 `0/25/50/75/100%` 关键帧；达到参考后连续打开/返回 5 次再决定是否需要快速节奏。低性能设备优先保留共享图片的空间连续，削弱模糊与局部 shader，不能先删掉对象连续性。
- **已确认：** 功能介绍第一视觉层包括：语境划词解释与翻译、全文翻译与摘要、精选外刊与分类、正文与网址自主导入、生词本与原句积累、Anki 协作、跨设备同步。保存文章与阅读进度没有被删除，只是本轮未选入第一视觉层。
- **已确认：** 完整能力正常展示；需要登录的能力旁只放极短的“登录后”标记，不用大段权限说明打断叙事。
- **文案待确认：** 用户认可“与 Anki 协作”这一方向，但需要让不了解 Anki 的用户也明白：阅读中积累的词可以被带入持续复习，而不是只看到一个陌生软件名。
- **已确认，覆盖老用户路径：** 功能介绍卡片只属于新用户/游客首页，登录用户首页不重复；使用方法进入使用说明。
- **待调研和视觉确认：** React Bits 等参考中与滚动堆叠、覆盖和位移接近的实现只能作为动效候选，不自动成为最终 UI；需要先做可见对比，再确定三个章节的构图、速度、遮挡程度和重复使用体验。
- **已确认：** 如果用户有明确喜欢的卡片运动或构图录屏，可以继续作为规范输入；但第一版可评审真实稿不以用户必须先找到参考为前提，Codex 应先做连接真实内容的提案供纠偏。

## 注册、账户与更新记录

- **已实现，待真实注册复验：** 注册先完成手机号、昵称与密码等基础账号创建，再在同一对话框进入可跳过的第二步填写英语水平、阅读偏好、出生年份和性别，避免首屏表单过长。
- **已确认：** “高中、四级、六级、考研、雅思/托福”表达用户当前最接近的英语阅读水平，不代表正在备考或获得证书。
- **部分实现：** 账户详情已显示并允许修改英语水平与阅读偏好；出生年份和性别可分别清除但暂不直接修改。昵称与六位数字密码的用户自助短对话框仍未实现，手机号验证码与找回密码仍按第 19 轮明确暂缓。
- **已暂缓：** 手机号验证码、短信资质、验证码登录、忘记密码与旧账号迁移整组第 19 轮未作选择，用户明确要求暂时不考虑。当前仍维持“未验证手机号标识 + 六位数字密码”的 Beta 边界，不得把短信写成正在实施。
- **已确认，待实现：** 更新记录进入 Menu 的使用说明内容区，首页“持续完善”链接直接打开该章节。
- **已确认：** 下一轮正式文案按每个位置 3 组完整候选交付，并说明语气差异与淘汰理由，由用户逐项把关。

## 仍待后续访谈

- **已确认，本地精修待真实动效验收：** 登录用户每次全新打开或刷新网站时播放一次约 `1.3–1.6s` 的 Ballpit 快速开场；同一次浏览中从 Reader 或功能层返回首页、以及游客中途登录，都不播放。第一帧先短暂保留自由碰撞，随后同一批球从触发瞬间的位置进入多层轨道同步旋转收拢。第 21 轮确认中段螺旋已可辨识，但否定扁椭圆终团和左右两拨的开孔；本地现改为更圆的球面采样终团并向四周径向开孔。覆盖层从首帧不透明，工作台不能先露出再被动画盖住。
- **对照稿，未做方向选择：** 保留并继续优化原螺旋的同时，提供 `opening=wordfall` 独立对照：彩色小球从视口上方落下，在中央拼成 `Context Reader`。该稿只用于比较品牌记忆、字形、球大小和节奏，未获用户选择前不能替代主开场。
- **已确认，待真实动效验收：** 中心球团随后向四周开孔，工作台从孔内显现；动画期间页面导航也应被首帧遮罩盖住，不能泄漏工作台或按钮。动画使用固定短时长，工作台数据并行加载；动画结束仍未就绪时承接克制骨架状态。鼠标点击、触摸点按、键盘确认键或主动滚动等明确操作都可在约 `150–250ms` 内把当前路径快速收束到结束帧并立即开放工作台，不能直接闪切。
- **已确认，待真实动效验收：** 手机保留同一多层收拢与开孔拓扑，但减少球数并略微加速；系统减少动态模式不播放完整卷入，只以中心球团短淡入淡出进入工作台。以上只冻结运动结构，球体材质、数量、精确速度、背景和最终视觉仍必须通过桌面、手机、重复使用与关键帧对比验收。
- 登录中途、Reader 返回与首页来源恢复的完整状态矩阵。
- 全站屏间连续性：外刊到导入、导入到功能卡片、功能卡片到封底在背景、层级、滚动速度、元素接力和手机降级上的完整时间线。
- 游客展示区中标题、图形、导入器和功能卡的进入方式；它们需要随滚动自然出现，而不是所有内容静态等在页面上，但每段必须使用不同空间动作且共享少量速度与色彩线索。
- 游客权限提示、剩余额度提示和登录对话框的最终视觉与文案。
- 自主导入模式切换器的真实 UI，以及未来图片/PDF 能力是否立项。
- 单独查词左侧停泊区在窄桌面上的降级方式，以及 Reader 工作区精确偏移和小窗初始尺寸。
- 首页末段的正式文案、微信二维码素材、更新记录具体承载与反馈表单展开视觉。
- Menu、首页悬浮高频按钮与 Reader 固定高频按钮的最终视觉。
- 除第一屏主句外的全站正式文案。
- 所有复杂动效的真实桌面、手机、键盘、低动态和重复使用验收。

## 已被覆盖，禁止复活

- 实体书隐喻、封面翻页、内页翻页、滚轮翻页、自动收尾和滚动边界仲裁。
- `/home-lab`、纯静态假首页或先完成视觉再补真实功能。
- 满屏 Ballpit、明显硬边框球场和标准左右分栏 Hero。
- 精选外刊总量只有 5–6 篇、双列 6 篇或标准行只留一篇。
- 因图片变小而预先削弱 Lusion 3D 与共享图片转场。
- 首页桌面整高左栏、常驻紧凑栏、在左栏写完整用途说明、把 Menu 放进左栏。
- Reader 使用 `Context Reader` 胶囊唤出高频入口。
- 手机左边缘把手、左栏或左右两套导航入口。
- 首版设置与无障碍菜单、用户可调阅读排版或动效设置。
- 把出生年份、性别误写成无需告知用途的非个人信息。
- 登录成功后要求用户再次点击刚才的保存/生词动作，或重复执行同一待续接动作。
- 登录瞬间用账号偏好重排游客当前正在看的推荐页。
- 手机两行 `Context Reader` 品牌按钮。
- 登录用户进入独立“完整外刊库”路由、主工作区或覆盖层。
- 为外刊来源再新增一个 Reader 返回按钮。
- Reader 返回无条件直达彩球封面、或登录成功后强制切换到老用户首页。
- Reader 头图落地后淡出、立即消失或卸载。
- 返回 Reader 来源卡片的完整或压缩反向共享图片转场。
- 登录老用户把外刊橱窗放在有记录用户的继续阅读之前。
