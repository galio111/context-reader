# Automated editorial publication

Accepted production: `20260920T172600`, parent `20260920T104100`, source `0a721d9997f9052b7d7d94dd433246098ab7cb93`. Public connectivity and the active server symlink match. DeepSeek, the 06:00 Asia/Shanghai schedule, 150 attempt limit and **¥1/day** budget are enabled. Today remains **30/35** because previous spending had already reached about ¥1.638; it was seeded into the new ledger rather than reset. A budget-blocked manual batch added no model requests (384 executions before and after) and SMTP accepted the 30/35 cost/shortfall email. All 30 articles remain visible on the public homepage with valid review/content hashes and 64 readable first-party images.

## Current cost and balance policy

A validated optional `budgetTrial: { day, cny }` applies only to the exact Shanghai ledger date; the base `dailyBudgetCny` remains unchanged. The final authorized 2026-09-21 trial uses cny: null: no application-side daily/ monthly Jev spend or attempt ceiling on that date. All other dates retain CNY 1/day and USD 4/month without a reset job. Provider credit limits and finite verified source supply remain real constraints. Source batches request at most the remaining daily article count. Emails split provider costs, fully duplicated DeepSeek validation cost, residual cost excluding validation, and per-check sample agreement. An opt-in jevAutoAdopt policy enables only checks meeting the documented sample gate after the completed trial date; confident approved checks skip duplicate text questions, uncertain/unavailable/unapproved checks use DeepSeek. See editorial-cost-and-jev.md for thresholds and accuracy limits.

Daily target is 35, with the user-confirmed **¥1/day** pipeline budget. Pre-request durable reservations cover classification, repair, text/image review, retries and Jev shadow calls; known usage settles the reservation, unknown outcomes retain it. Completed days stop all further paid reserve work. Daily email includes actual estimated cost, token totals, stages and unknown reserved cost. See [cost audit and Jev setup](editorial-cost-and-jev.md) for the incomplete historical accounting and exact limits of the daily forecast.

Classification uses full article text, validates every topic/category and requires two verbatim body evidence excerpts; malformed classification cannot silently fall back to keyword categories. Business preferences now use metadata. CET-6/考研 and IELTS/TOEFL foundation share the same preference tier. Daily category deficits target business/current/science/culture 9/9/9/8, capped at 10 each, and the two language tiers at 18 each; these are selection constraints, never permission to relabel.

Guardian business/world and Crunchbase have offline extraction/image evidence and passed production source verification; both are enabled. Three existing article classifications were corrected after full-text reading: Navy procurement and Vals to business, India caller-ID regulation to current affairs. The resulting current-day homepage counts are business 4, current 6, science 7 and culture 13; this correction does not claim that the existing day now has balanced proportions. Guardian footer and Crunchbase tags have specific cleanup rules. Econlib/OECD passed some body/image samples but insufficient recent cadence prevents claiming daily supply; Wharton, Fast Company, LSE, ADB and France24 failed current extraction/access checks.

## Publication and supply

The daily target counts new `autoPublishedAt` timestamps on the Shanghai calendar day, not manual/bulk publications or existing inventory. Manual/single-source discovery and URL/paste/saved-article intake retain the candidate workflow. School/CET-4 and lower labels remain available but do not count toward the automatic target. A source's lower-level hint alone no longer excludes an article: full-text classification decides its actual label, with Pro confirmation for proposed low levels.

Each article needs at least 401 English words, safe complete images, acceptable full-text quality, freshness and duplicate checks. Actual time-sensitive stories expire after seven days; a business topic alone does not make an evergreen essay time-sensitive. Verified weekly sources are usable without inventing new publication dates. WordPress feed pages 1–3 provide bounded supplementation, with normal robots, hostname and duplicate checks. Deleted-audit URLs remain excluded; this follow-up does not restore deleted articles.

A database lease serializes batches. Up to three attempts are reserved before network work, then unused reservations are reconciled only after a completed call; crashes retain their reservation. Recent unresolved, stale-version, expired or content-mismatched candidate approvals receive bounded revision-guarded retries, at most two per candidate/day. Site visit limits and an overall attempt limit bound cost. After 35 publications, paid work stops. Existing approved candidates remain usable under their 48-hour approval limit.

Seven-day deficits rank candidates. The first source pass caps source/primary-topic counts at 4/6; supplementation permits 6/10. The two effective difficulty tiers are capped at 18 each, combining CET-6/考研 with IELTS/TOEFL foundation. These limits diversify the actual supply and never change labels to fill a bucket. Homepage primary topic and its four presentation categories are separate metadata. On the accepted day, primary topics were science 9, nature 7, culture/history 7, business 3, growth 2, society 1 and literature 1. Difficulty was CET-6/考研 15, IELTS/TOEFL advanced 15, foundation 0. A complete 30-article day is verified; stable multi-day output and uniform supply across all three adult labels are not yet established.

Publication re-reads the row, verifies policy version 2, explicitly completed approval, a canonical hash of title/text/ordered blocks, an approval age under 48 hours and fresh source dates. Compare-and-swap protects edits. Strict safe image storage must succeed. A pending-curation journal repairs interruption between publication and homepage placement. Featured selection is random without another AI call.

## Repair before judgement

Standalone website furniture must be repaired before condemning an otherwise substantive article. Cheap furniture signals first determine whether repair is worth attempting; absence of these signals never grants publication and the full model audit still runs. Flash and Pro independently propose removals using full ordered blocks (maximum 80,000 characters), and must agree on the exact block id, text and category. At most 16 blocks and 20% of words can be removed; at least 401 words must remain. Images, captions, quotations, tables and mixed substantive blocks are protected. Retained prose is not rewritten. Original/final hashes and exact removed blocks remain in review metadata. Failed repair grants no approval. Every repaired article is reclassified and fully reviewed.

Two offline snapshot fixtures passed after removing a related-story link and fundraising furniture; retained paragraphs and images were exactly identical. These evaluations did not mutate or restore deleted articles. Live publication also exercised repaired content. Important reproduced extraction corrections:

- Popular Science's visible, publicly delivered `.entry-content.Article-bodyText.paywall` story body was mistaken for an overlay. One article went from a 41-word fragment to 887 words and nine images. A domain-specific exception retains that body; hidden content and actual subscription overlays remain filtered. All nine images passed the full review.
- A sponsored **other-story recommendation card** incorrectly condemned an independent Popsci article. Sponsorship warnings now distinguish the linked card from the current article's own label; actual sponsored stories still fail.
- Global Voices' `Categories` footer is bounded separately from its story. Its sampled images remained unreadable, so the source was not enabled on that evidence. Publisher crawl delays up to 30 seconds are respected, never bypassed.

The text review assesses truncation, contamination, missing-image captions, media dependence and promotion across all chunks (up to 12), never just head/tail samples. Flash classifies; uncertain classification and all proposed school-level results escalate to Pro. Actual images are assessed with Flash in batches of three, up to 20 images. Image count alone is not a gallery verdict. **V4 Pro is text-only.** Conflicting image judgements remain pending. Caption checks pair the full ordered article with actual pixels and require two explicit missing-image confirmations; empty alt text or distant credits alone do not prove missing images. Request failure and uncertainty never authorize deletion.

Completed confirmed automatic defects enter the reversible rejected queue, not hard deletion. `recommendation_editorial_excluded_urls_v1` retains up to 5,000 canonicalizable URLs from explicitly deleted audit failures. Explicit manual URL intake remains possible. Existing Admin mutation APIs accept `expectedUpdatedAt` revision guards.

## Notifications and provider cost

Daily terminal email reuses the existing private site notification recipient. It reports the actual uploaded count, four homepage categories, seven exclusive primary topics, every adult difficulty (including zero), repair count and each title/source link. A shortfall is explicitly labelled failure. The date/result delivery key prevents routine repeats after accepted SMTP delivery; failed sends retry after a 15-minute delay. SMTP acceptance does not prove inbox placement. The accepted day first reported 28/30, then sent a distinct 30/30 success report after further verified supplementation.

Normal runtime is DeepSeek with 150 daily attempts. The Admin range is 30–240; this maintenance day temporarily used 240 and stopped at 160 cumulative attempts, then restored 150. Earlier attempts were not erased. The new daily ¥1 reservation ledger additionally enforces a monetary ceiling. Online classification/review execution records from 10:00 through acceptance estimated **$0.143768** while adding 22 articles to the eight already published. This excludes earlier work, offline probes, transport/storage and hosting, so it is not a measured full-day or invoice total. The old estimate is superseded by the ¥1/day limit and the explicitly conditional forecast in editorial-cost-and-jev.md; 35/day under this ceiling is not yet a verified multi-day result.

Optional `AI_GATEWAY_API_KEY` enables `jev-shadow` through `experimental_evaluate`, model `typesafe-ai/jev`, for five boolean probabilities. Jev is advisory on the trial date. Subsequent selective use requires the persisted completed-trial calibration gate; actual images, classification, difficulty and repair remain with DeepSeek. Missing credentials, failures, invalid answers and exhausted budget leave DeepSeek active. The production runtime now has the Gateway key and selects `jev-shadow` (2026-09-20). Authenticated Admin confirms both providers configured; model-list access succeeds from the mainland server. No inference was performed because the daily budget was already exhausted; inference entitlement, accuracy and card-free credit remain unverified. The conservative pipeline-only monthly reserve is at most $4 using the previously researched $0.042/M rate; other Gateway activity and changed pricing are outside this reservation. Shadow calibration adds calls rather than immediately reducing DeepSeek costs.

## Verification and retained audit evidence

145 regressions, production build, protected release contracts and egress guard passed for the cost/balance release. Live account/session, protocol-2 sync, unauthorized Admin rejection, homepage/images and seven-service health were rechecked. Paid reading/dictionary/translation and forced-provider smoke calls were not repeated after the user cost ceiling; their same-day prior-release evidence is retained, not presented as a new-release rerun; disposable accounts were removed. The active public release/symlink and accepted parent rollback image match. The existing fresh database backup had restored all 16 public tables successfully. Only app/caddy were recreated. Private operational snapshots and the 30-article acceptance record are under `/var/backups/context-reader/manual/editorial-20260919/`; local evidence is under ignored `artifacts/editorial-audit-20260920/`.

The earlier 08:45 cleanup snapshot remains historical evidence: 163 candidate publications, 65 candidate deletions, 52 published deletions, 346 published rows and 18 active candidates; two confirmed low candidates stayed unpublished, while 16 candidate and 16 published uncertain cases were retained. The 113 deleted URLs remain excluded. This follow-up does not repeat that cleanup. Personal saved articles and vocabulary are outside scope.

Nautilus, Noema, Quanta and Nieman Lab have production verification and are enabled. Other source probes may fail for robots, extraction, images or freshness and are not enabled merely to reach a number. Neither model confidence nor passing this pipeline certifies objectively exact exam difficulty or perfect extraction. The report exposes real counts and unresolved supply limitations instead of relabelling articles or declaring an unverified long-term guarantee.
